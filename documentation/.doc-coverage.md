# Documentation Coverage Report

Generated: 2026-05-13 (doc-updater cycle 2 on branch `develop`; appended after spec-reviewer's second pass)

---

## Summary

| Severity | Count | Action |
|---|---|---|
| CRITICAL | 0 | - |
| HIGH | 0 | - |
| MEDIUM | 5 | Auto-fixed: 4; Escalated: 1 |
| LOW | 3 | Deferred (budgets, all have AD46 exemptions) |

---

## Auto-fixed findings

### MEDIUM: Pass 5 — configuration.md Worker Secrets missing template columns

`documentation/configuration.md` `## Worker Secrets` section used a `| Secret | Description |` two-column table that lacked the required grouped-table columns: `Variable`, `Required`, `Consumed by`, `Implements`. Auto-fixed by restructuring to the five-column template shape with all required headers.

### MEDIUM: Pass 5 — configuration.md Worker Vars missing template columns

`documentation/configuration.md` `## Worker Vars (non-secret)` used a `| Var | Default | Purpose |` table missing `Required`, `Consumed by`, `Implements` columns. Auto-fixed by restructuring to `| Variable | Default | Required | Consumed by | Implements |`.

### MEDIUM: Pass 5 — deployment.md Production Deployment missing template fields

`## Production Deployment` section lacked `**When:**`, `**Command:**`, `**Verifies:**`, `**Rollback:**` fields. Auto-fixed by adding all four fields including rollback procedure with `wrangler deployments list` + `wrangler rollback <id>` commands.

### MEDIUM: Pass 5 — deployment.md Local Development missing template fields

`## Local Development` section lacked template fields. Auto-fixed.

### MEDIUM: Pass 5 — deployment.md Integration deployment missing template fields

`## Integration deployment` section lacked template fields. Auto-fixed.

---

## Cold-read gaps (Pass 10)

### deployment.md — FAILED before fix

**Simulated task:** "Roll back the last production deploy. Output exact commands in order, including verification commands between steps."

**Gap:** No rollback commands existed in the file before this pass. A stranger reading the file had no way to answer the task.

**Fix applied:** Added `**Rollback:**` field to `## Production Deployment` with `wrangler deployments list` and `wrangler rollback <deployment-id>` commands, plus a note that D1 migrations are forward-only and may require a reverse migration.

### configuration.md — PARTIAL

**Simulated task:** "List every env var the dev-bypass code path consumes. Output: name, type, default, where consumed."

**Gap:** The Worker Secrets table previously lacked "Consumed by" (source file) information. Fix applied (Pass 5 restructure above) adds the source file paths for `DEV_BYPASS_TOKEN`, `DEV_BYPASS_USER_ID`, and `IS_PRODUCTION`.

---

## Deferred LOW findings (Pass 2 — file budgets)

All three oversize files carry AD46-backed `doc-allow-large` exemptions:

- `documentation/architecture.md`: 348 lines vs 350 budget (0.99x). **Within budget** (prior passes trimmed the oversize bullets; AD46b exemption no longer needed but retained for future diagram growth).
- `documentation/deployment.md`: 359 lines vs 200 budget (1.80x after fix). **Exempt per AD46a** (colocation of deploy runbook, dev-bypass, and admin-routes).
- `documentation/decisions/README.md`: 1359 lines, 47 ADRs. **Exempt per AD46c** (single-file ADR design).
- `documentation/api-reference.md`: 697 lines vs 600 budget (1.16x). LOW. No exemption. Approaching MEDIUM threshold; monitor.

---

## Proposed-move plans (Pass 4)

### deployment.md Dev-bypass runbook `**Failure-mode quick-reference:**` table

The `Symptom / Cause / Fix` table at line ~319 of `deployment.md` matches the troubleshooting-recipe signature that belongs in `troubleshooting.md`. However, AD46a explicitly endorses deployment.md colocation for operator runbook content that would fragment the linear deploy workflow. This specific table provides immediate failure-mode guidance for the dev-bypass procedure immediately following the runbook steps. **Deferred** under AD46a rather than moved; operator judgment call.

---

## Pass 3 — Implementation prose (false positives)

Lines matching implementation-prose heuristics that are NOT AC leakage:

- `architecture.md` line 86: "must" in source-module table (descriptive of gate behavior, allowlisted for architecture docs).
- `architecture.md` line 389: "MUST NOT" in Pattern B script convention (legitimate implementation constraint for doc consumers, not an AC bullet).
- `deployment.md` line 79: "must match" in operational runbook context (valid in deployment.md).

No AC leakage found. Pass 3 clean.

---

## Cycle 2 findings (2026-05-13 — after spec-reviewer second pass)

### MEDIUM (auto-fixed): Pass 2 — spec backlinks for REQ-PIPE-010..014

Five `Status: Implemented` REQs had no doc backlink after spec-reviewer's second pass re-promoted them:

- `REQ-PIPE-010` (body-fetch for thin feed snippets) - sub-feature of coordinator pipeline
- `REQ-PIPE-011` (candidate filtering rules) - sub-feature of coordinator pipeline
- `REQ-PIPE-012` (same-story matching policy variants) - sub-feature of finalize consumer
- `REQ-PIPE-013` (same-story cross-tick automation and retention coupling) - sub-feature of finalize consumer
- `REQ-PIPE-014` (same-story operator surfaces) - implemented by dedup-sweep-consumer + admin routes

**Fix applied:** Added backlinks to `documentation/architecture.md` section 4.5 table rows:
- `scrape-coordinator.ts` row: added REQ-PIPE-010, REQ-PIPE-011
- `scrape-finalize-consumer.ts` row: added REQ-PIPE-012, REQ-PIPE-013
- `dedup-sweep-consumer.ts` row: added REQ-PIPE-014

Architecture.md line count after edit: 348 (0.99x of 350 budget, within budget).

### Pass 1-4, 6-10: No new findings

- Pass 1: All table cells within 50-word budget (OK)
- Pass 2: All files at/within budget (architecture.md 0.99x, api-reference.md 1.16x LOW monitored, deployment.md 1.29x LOW AD46a-exempt, decisions/README.md AD46c-exempt)
- Pass 3: No AC-style implementation prose in non-decisions files
- Pass 4: No lane violations. No dual-narrative ADRs. All 47 ADRs have required Decision + Status fields.
- Pass 5: Tombstoned endpoint sections (GET /api/digest/:id, POST /api/digest/refresh) are intentionally stripped - exempt from format template enforcement.
- Pass 6: All 10 **Verification:** fields in security.md cite existing test files; all REQ IDs verified present in cited files (6/6 cross-checks PASS).
- Pass 7: All 88 **Implements:** REQ references in docs resolve to sdd/ REQs (0 mismatches).
- Pass 8: All 8 **Path:** routes resolve to source handlers. CLOUDFLARE_API_TOKEN / CLOUDFLARE_ACCOUNT_ID are CI/CD secrets (not Worker src/ env vars) - not stale.
- Pass 9: No trim operations this pass.
- Pass 10: api-reference.md PARTIAL (no explicit curl example for session-auth endpoints - acceptable; path present in code block, Conventions section explains authentication). All other files PASS.
