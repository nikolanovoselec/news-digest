# Last `/sdd clean` Run

**Date:** 2026-05-13 (later pass)
**Mode:** unleashed
**Scope:** all (full `sdd/` corpus)
**Branch:** develop
**Triggered by:** user (`/sdd clean --all`)

## Execution manifest

Per the binding execution contract in `~/.claude/rules/spec-discipline.md`, every spec-side rule below MUST execute. `pending` rows at finalize time are a self-emitted HIGH finding.

### Spec-side (spec-discipline.md)

| Rule | Required action this run | Status |
|---|---|---|
| Forbidden content in REQs | Walk every Active REQ; flag banned tokens in AC/Intent. | ran (68 Active REQs walked, 0 hex / CSS / src-path / function-call findings) |
| Status field semantics + Deprecated discipline | Walk every REQ; verify Status shape + Deprecated `Replaced By:` / `Removed In:`. | ran (78 REQs walked, 10 Deprecated all carrying `Replaced By:` + `Removed In:`, 0 findings) |
| REQ rendering template (binding) | Walk every Active REQ; verify the binding render shape. | ran (68 Active REQs walked, 0 findings) |
| REQ length guidance | Walk every Active REQ; flag length tiers. | ran (68 Active REQs walked; max 34L on Deprecated REQ-PIPE-008 [exempt]; max 26L on Active REQ-GEN-005, all under LOW threshold) |
| Acceptance criteria guidance + REQ accretion guard | Walk every Active REQ; flag AC count > cap. | ran (68 Active REQs walked, 0 findings — all Active REQs ≤7 ACs; only REQ-PIPE-008 at 11 ACs is `Status: Deprecated` and exempt) |
| Actor coherence | Walk every Active REQ; flag actor-axis violations in ACs. | ran (68 Active REQs walked, 0 findings) |
| Sub-bullets in ACs are banned | Walk every Active REQ; flag indented list items under ACs. | ran (68 Active REQs walked, 0 findings; REQ-AUTH-001 sub-bullet shape mentioned in 2026-05-13 archive no longer applies — current AC 1-6 are flat) |
| Cross-cutting concerns get their own REQ family | Walk every Active REQ; flag policy-shape ACs in feature REQs. | ran (68 Active REQs walked, 0 findings; rate-limit policy cleanly isolated in REQ-RATE-001) |
| Run-on AC bullets | Walk every AC; flag >150 words or ≥3 semicolons outside enums. | ran (≈240 ACs scanned, 0 findings) |
| Mechanism leakage in AC bullets | Walk every Active REQ; flag mechanism tokens. | ran (68 Active REQs walked, 0 findings under the canonical leak vocabulary: HttpOnly / SameSite / Secure / Path=/ / Max-Age / Cf-Access-* / X-Forwarded-For / X-Request-Id / csrfMiddleware / rateLimiter / requireAuth / RS256 / HS512 / AES-256-GCM) |
| Changelog drift | Diff `sdd/changes.md` against AC-changed diff hunks. | ran (no AC changes this pass; 0 drift) |
| Meta-content leakage Rule A (stub-after-extraction) | Walk every REQ; flag stub-after-extraction shape. | ran (78 REQs walked, 0 findings) |
| Meta-content leakage Rule B (Notes two-shape) | Walk every REQ; flag Notes-vs-two-shape violations. | ran (Notes fields on REQ-AUTH-001/006/008/010, REQ-PIPE-001/008, REQ-MAIL-001/003 scanned; all conform to doc-pointer two-shape) |
| Meta-content leakage Rule C (preamble edit-history) | Walk every `sdd/{domain}.md` preamble; flag edit-history prose. | ran (11 domain files walked, 0 findings) |
| Test coverage and enforce_tdd | Run all three classification passes. | ran (`enforce_tdd: true`; 63 Implemented REQs walked; 0 auto-demotes; 0 test-quality findings) |
| CQ-1 — REQ-test truth-check | For every Implemented REQ, walk every test file containing the REQ ID; verify name + content. | ran (63 Implemented REQs walked, ≥220 test-file references opened, all 11 previously-demoted REQs verified — describe-block citations confirmed for REQ-MAIL-003, REQ-PIPE-010..016, REQ-HIST-003, REQ-READ-008, REQ-SET-008; 0 findings) |
| CQ-2 — Vendor / external-interface drift | For every Implemented REQ, extract vendor tokens; grep source for each. | ran (63 Implemented REQs walked, 16 vendor tokens scanned including Cloudflare / Resend / OAuth / OIDC / GitHub / Google / D1 / KV / Queue / Workers / Hono / Astro; 0 orphans in AC text) |
| CQ-3 — Content-preservation on shrink | On every shrink op proposed this run, run tokenization check. | inert (no shrink ops proposed this run; clean tree, no diff) |

## Findings summary

| Severity | Count | Auto-fixed | Escalated | Deferred |
|---|---|---|---|---|
| CRITICAL | 0 | - | - | - |
| HIGH     | 0 | - | - | - |
| MEDIUM   | 0 | - | - | - |
| LOW      | 0 | - | - | - |
| JUDGMENT (informational) | 1 | - | 1 | - |

## Auto-fixed

None. The spec is at steady state after the prior 2026-05-13 cycle-5 sweep, the cycle-4 CQ-1 escalation resolution (commit `c6cd951`), and the REQ-OPS-006 re-promotion (commit `e1b916c`). All structural rules pass on the active REQ corpus.

## Escalated to `sdd/.review-needed.md`

- **JUDGMENT informational — storage-shape token references across 11 Implemented REQs are not enumerated in any ADR `Overrides:` header.** AD9 ("Storage-shape names are the persistence contract") was the prior register; AD9 has since been superseded and the only surviving `**Overrides:**` line in `documentation/decisions/README.md` enumerates `REQ-AUTH-001` for `behavioral-policy` and `workers-dev-exposure` (not storage-shape). Tokens like `revoked_at` (REQ-AUTH-008), `pending_discoveries` / `added_at` (REQ-DISC-001), `email_enabled` (REQ-MAIL-003), `hashtags_json` / `digest_hour` (REQ-SET-001), and `scrape_runs` (REQ-PIPE-001) read as persistence contract, not gratuitous mechanism leakage — but the active forbidden-content allowlist in `sdd/config.yml` does not explicitly admit them and the post-supersession ADR register has no override line covering them. No spec edit applied this pass: this is the conservative `unleashed` default ("never overwrite intent"). Owner decision: either reinstate a successor ADR with a single `Overrides:` line naming all 11 REQs for the `storage-shape-token` category, or accept the post-AD9 carveout as folkloric. Sister item `**Status:** Implemented` REQ-OPS-002 references error-code tokens (`llm_invalid_json`, `llm_failed`, `all_sources_failed`, `generation_stalled`, `user_cancelled`, etc.) which are user-facing string contracts and admitted by `user_facing_strings: true` in `sdd/config.yml` — those are not part of this finding.

## Informational (no action)

- `forbidden_content_overrides` in `sdd/config.yml` remains `[]`. The JUDGMENT escalation above proposes regularising the post-AD9 override surface, but this is owner-only work.
- 134 test files inventoried under `tests/`; ≥220 REQ-ID literal references across Implemented REQs. CQ-1 nested-describe counter confirms zero AC-count-vs-test-count mismatches after honoring the describe-block parent rule.
- 10 Deprecated REQs (REQ-PIPE-008, REQ-GEN-001..008, REQ-READ-004) all carry both `Replaced By:` and `Removed In:` fields. Per the discipline carveout, structural rules (REQ length, AC count, run-on AC) are inert on Deprecated REQs.
- Round counter input: last 3 commits all carry `[sdd-clean]` prefix, all excluded per the rule. Round counter = 0 at start of this run.

## Working tree changes

- `sdd/.last-clean-run.md` — this file (final manifest + audit log for the second 2026-05-13 pass).
- `sdd/.review-needed.md` — one new dated entry capturing the JUDGMENT informational item above.

## Rules applied this run

- `spec-discipline.md` Binding execution contract — full 18-row spec-side manifest executed.
- `spec-discipline.md` Round-counter exclusions — `[sdd-clean]` prefix excluded; round counter ends at 1 (this commit's `[sdd-clean]` prefix is also excluded by the same rule).
- `spec-discipline.md` `unleashed` conservative default — JUDGMENT escalated, not auto-resolved; never overwrite intent.

## Next steps (spec-side)

- 1 per-category commit on `develop` tagged `[sdd-clean]`:
  1. Audit log + review-needed update (`sdd/.last-clean-run.md` + `sdd/.review-needed.md`)
- `doc-updater` runs after spec-reviewer per the rule's "Spec/docs/code lane separation" section.
- CI monitor per `~/.claude/rules/ci-monitoring.md` after the user-driven push.
- User-only merge to `main`.

---

## Doc-side audit log (doc-updater — 2026-05-13 pass after spec-reviewer second pass)

**Agent:** doc-updater
**Mode:** unleashed
**Scope:** all (full `documentation/` corpus)
**Triggered by:** `/sdd clean --all` (sequential, after spec-reviewer)

### Doc-side execution manifest (documentation-discipline.md)

| Pass | Required action | Status |
|---|---|---|
| Pass 1 — Per-cell word budget | Walk all Markdown tables; flag cells > 50 words | ran (all files walked; 0 violations) |
| Pass 2 — Per-file line budget | Count non-blank non-fence lines per file; apply budget table | ran (api-reference 1.16x LOW monitor; deployment 1.29x LOW AD46a-exempt; decisions 3.43x AD46c-exempt; architecture 0.99x OK; all others OK) |
| Pass 2 hatch audit | Verify all `doc-allow-*` markers reference AD46 | ran (13 markers audited; all reference AD46 family; 0 bare/unjustified markers) |
| Pass 3 — Implementation-prose | Scan for must/shall/ensures-that AC-style patterns | ran (58 pattern matches; all false-positives: ADR Consequences, parameter constraints, deployment runbook; 0 real AC-leakage in non-decisions files) |
| Pass 4 — Lane-violation | Check HTTP triplets in wrong files; deploy commands in wrong files; threat-model in wrong files; decision rationale in wrong files | ran (0 violations; existing Symptom/Cause/Fix table in deployment.md deferred under AD46a) |
| Pass 4 — ADR dual-narrative | Detect two Decision headings, retro-edit phrases, Status/body mismatch | ran (47 ADRs walked; 0 dual-narrative findings; all ADRs carry Decision + Status) |
| Pass 4 — ADR reclassification | Identify non-ADR content in decisions/ | ran (all 47 ADRs have required Decision + Status fields; 0 reclassifications needed) |
| Pass 5 — Format template | Walk every `##`/`###` section; check required fields per lane | ran; tombstoned endpoint sections (GET /api/digest/:id, POST /api/digest/refresh) exempt — intentionally stripped to Status + Replacement only; 0 real violations |
| Pass 6 — Verification truth-check | For every `**Verification:**` field in docs, check cited test file contains section's REQ ID and content-word tokens | ran (10 fields in security.md; all 10 test files exist; 6 REQ cross-checks all PASS) |
| Pass 7 — Implements-vs-AC cross-walk | For every `**Implements:**` field, classify doc section against linked REQ's Intent + ACs | ran (54 fields, 88 REQ references; all 88 resolve to sdd/; 0 mismatches; 0 low-confidence flags) |
| Pass 8 — Stale code-block detection | Check **Path:** routes, function signatures, JSON shapes, env vars against src/ | ran (8 routes all resolve; 42 env vars: CLOUDFLARE_API_TOKEN/ACCOUNT_ID are CI/CD vars not Worker src/ vars — not stale; 0 stale findings) |
| Pass 9 — Content-preservation on trim | Run on any Pass 1 trim operations | inert (no trim operations this pass) |
| Pass 10 — Stranger cold-read | Simulated cold-read against task registry | ran; api-reference PARTIAL (no explicit curl example — acceptable, path in code block + Conventions explains auth); all others PASS |
| Phase 2 — Spec backlinks | Every Implemented REQ must have at least one doc mention | ran (64 Implemented REQs; 5 missing backlinks found: REQ-PIPE-010..014; auto-fixed in architecture.md section 4.5) |
| Phase 2 — Index consistency | Every file listed in documentation/README.md exists; no orphans | ran (7 doc files + .doc-coverage.md; all index entries resolve; no orphans) |
| Phase 2 — Broken links | Every cross-reference in docs resolves | ran (all `[../sdd/...]` backlinks verified by REQ-ID resolution check; 0 broken) |

### Doc-side findings summary

| Severity | Count | Auto-fixed | Escalated | Deferred |
|---|---|---|---|---|
| CRITICAL | 0 | - | - | - |
| HIGH     | 0 | - | - | - |
| MEDIUM   | 5 | 5 | 0 | 0 |
| LOW      | 2 | 0 | 0 | 2 |

### Doc-side auto-fixed

- MEDIUM x5: `documentation/architecture.md` — spec backlinks added for REQ-PIPE-010, REQ-PIPE-011, REQ-PIPE-012, REQ-PIPE-013, REQ-PIPE-014 (section 4.5 table rows for scrape-coordinator.ts, scrape-finalize-consumer.ts, dedup-sweep-consumer.ts)

### Doc-side deferred LOW

- `documentation/api-reference.md`: 697 lines (1.16x budget). No exemption. Monitor; MEDIUM threshold is 1.4x.
- `documentation/api-reference.md` Pass 10: PARTIAL cold-read (no curl example for session-auth endpoints). Acceptable given Conventions section explains auth pattern; operator-facing runbook has explicit curl examples.

### Working tree changes (doc-side)

- `documentation/architecture.md` — 3 table rows updated with REQ-PIPE-010..014 backlinks
- `documentation/.doc-coverage.md` — cycle 2 findings appended, architecture.md budget note corrected
- `sdd/.last-clean-run.md` — this doc-side audit log appended
