# Last /sdd clean run

**Date:** 2026-05-13
**Mode:** unleashed
**Scope:** --scope=all (sdd/)
**Branch:** develop
**Working tree:** clean before run
**Round counter:** 0 (last 3 commits in sdd/ lane: `[sdd-clean]` no-op + plain `fix:` + plain `docs:` — none counted)

## Summary

One HIGH auto-applied: REQ-OPS-006 demoted Implemented → Partial because no test file literally cites the REQ ID. Six MEDIUM findings escalated to `.review-needed.md`: one CQ-1 name-only-match (REQ-OPS-005 force-refresh tests), five AC-vs-test count mismatches (test-authoring work owned by the developer, not auto-fixable), and one informational note on REQ-AUTH-008 column-name references not yet covered by AD9. CQ-2 (vendor drift) and CQ-3 (content preservation) are clean. Structural pass is clean across all 64 REQs.

## REQ corpus snapshot (post-edit)

| Metric | Count |
|---|---|
| Total REQs | 64 |
| Implemented | 52 (was 53; REQ-OPS-006 demoted) |
| Partial | 2 (was 1; REQ-OPS-006 newly Partial) |
| Deprecated (with Replaced By / Removed In) | 10 |
| REQs >50 lines (HIGH/MEDIUM length band) | 0 |
| REQs in 26-50 LOW band | 6 (REQ-PIPE-001, REQ-PIPE-003, REQ-AUTH-001 carry over from 2026-05-12 informational note; no change) |

## Findings by category

### Auto-applied (HIGH) — 1

- **REQ-OPS-006 (Integration deployment target):** Status Implemented but `enforce_tdd: true`'s strict literal-match rule finds no test file referencing the REQ ID. Auto-demoted to `Partial` with `Notes:` explaining the gap. The deployment topology (separate hostname, suffixed resources, manual dispatch, develop-only HEAD, no crons, fresh D1) IS exercised by the `deploy-integration` workflow in production but that does not count toward the literal-match rule. Behavioral observation → changelog entry.

### Escalated to `.review-needed.md` (MEDIUM) — 6

- **CQ-1 `req-test-name-only-match` — REQ-OPS-005 force-refresh tests.** `tests/pipeline/force-refresh.test.ts` covers AC 1+2+4 with real assertions (POST/GET, 303 redirect, JSON 200 happy path, reuse-window guard, three-layer admin gate). The REQ ID appears in the file header comment only, never in a `describe` / `it` block name. Conservative: NOT auto-fixed — test-file describe renames live in the test author's lane (same pattern as the cycle-1 `REQ-HIST-001 / REQ-READ-001 AC 7:` rename PR).

- **AC-vs-test count mismatch (5 REQs):** REQ-DES-001 (6/5), REQ-DISC-002 (3/1), REQ-OPS-005 (5/0), REQ-READ-005 (3/1), REQ-SET-005 (4/1). Each REQ has at least one test naming the REQ ID; the AC-count rule still flags the gap. No auto-fix — adding per-AC test names is test-authoring work.

- **REQ-AUTH-008 column-name leakage informational (JUDGMENT, no action this run):** AC 2/4/5 reference `parent_id`, `revoked_at`, `session_version`, `users.session_version`. AD9 already accepts the same persistence-contract pattern in REQ-AUTH-002, REQ-DISC-001/002, REQ-SET-001/005, REQ-MAIL-001 — REQ-AUTH-008 is the only sibling refresh-token REQ not enumerated. Two natural resolutions: (a) extend AD9's `Overrides:` to cover AUTH-008 (preserves load-bearing detail AD9 explicitly accepts), or (b) rewrite the AC bullets to omit column names (loses detail AD9 says is the contract).

### CQ-2 (vendor / external-interface drift) — 0

All vendor / protocol tokens in Implemented REQ ACs (`Cloudflare Access`, `GitHub`, `Google`, `Resend`, `Workers AI`, `OAuth 2.0`, `OIDC`, `JWT`, `D1`, `KV`, `Queues`, `Vectorize`) verified present in `src/**`, `wrangler.toml`, or `.github/workflows/**`. No orphaned references.

### CQ-3 (content-preservation on shrink) — n/a this run

No shrink-in-place edits were attempted: every REQ is at or below the 25-line OK threshold or in the 26-50 LOW band that policy already leaves alone. The two long ACs in REQ-AUTH-001 (AC 8 and AC 9) use the prescribed `a./b./c.` sub-bullet convention and are not run-on candidates.

### Tautology / skipped-test heuristics — 0 actionable

- Zero tautological assertions across the test corpus.
- 13 `test.skip(condition, 'reason')` lines in e2e specs are runtime fixture-availability guards (no articles in user's history yet, no hashtags configured, no DOM selector present) with documented reasons. These are conditional skips, not dead-code skips — they do not match the "skipped without justification" rule.

### ADR-formalised exceptions honoured (no findings emitted)

- Cookie attributes in REQ-AUTH-002 / REQ-AUTH-003 → AD8 (`mechanism-leakage:REQ-AUTH-002`, `REQ-AUTH-003`).
- Storage-shape names in REQ-DISC-001, REQ-DISC-002, REQ-AUTH-002, REQ-SET-001, REQ-SET-005, REQ-MAIL-001 → AD9.

## Documentation file sizes vs budgets

Unchanged from 2026-05-13 audit (no documentation edits — spec-reviewer lane is `sdd/` only):

| File | Lines | Soft budget | Status |
|---|---|---|---|
| documentation/decisions/README.md | 1358 | 100/ADR | Exempt (4 `doc-allow-large` markers in place) |
| documentation/api-reference.md | 692 | 600 | LOW (within 600-1000 band) |
| documentation/architecture.md | 428 | 350 | LOW (within 350-500 band) |
| documentation/deployment.md | 343 | 200 | LOW (within 200-350 band) |
| documentation/api-reference-admin.md | 252 | 250 | LOW (within 250-400 band) |
| documentation/configuration.md | 183 | 200 | OK |
| documentation/security.md | 154 | 250 | OK |
| documentation/README.md | 40 | n/a | OK |

These are doc-updater's lane; reported for context only.

## Commits this run

1. `[unleashed] [spec-reviewer] auto-demote REQ-OPS-006 to Partial (no test reference)` — touches `sdd/observability.md` + `sdd/changes.md`.
2. `[unleashed] [spec-reviewer] escalate cycle-4 CQ-1 + AC-vs-test mismatches to .review-needed.md` — touches `sdd/.review-needed.md` + `sdd/.last-clean-run.md`.

## Next likely actions

- Owner-side: rename a `describe` block in `tests/pipeline/force-refresh.test.ts` to include `REQ-OPS-005`, mirroring the cycle-1 `REQ-HIST-001 / REQ-READ-001 AC 7:` rename pattern. Clears CQ-1.
- Owner-side: decide whether to extend AD9 to cover REQ-AUTH-008 column-name references (the cleanest fit) or rewrite the AC bullets.
- Owner-side: optionally add `// Implements REQ-OPS-006` annotation in `.github/workflows/deploy-integration.yml` (or a thin test that asserts the workflow file's shape) so the auto-demote clears next pass.
- Otherwise the spec stays as-is until a real product change drives the next edit.
