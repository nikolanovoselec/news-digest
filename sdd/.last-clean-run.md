# spec-reviewer run — 2026-04-27 (round 3)

**Mode:** unleashed
**Trigger commit:** 7e6f3ea (fix(security): close TAGS_MUTATION bypass on PUT /api/settings)
**Branch:** review-2026-04-27/concurrency-correctness (not main/master — unleashed permitted)
**Round counter:** 1/3 (top-of-branch is a non-agent `fix(security)` commit; counter reset)

## Phase 0 — Triage

- SDD bootstrap detected (`sdd/README.md` present)
- Config: mode=unleashed, enforce_tdd=true (forced)
- Working tree had a stale `documentation/api-reference.md` edit; stashed for the duration of this run, restored at the end (it landed as commit `584e9e6` from doc-updater while spec-reviewer was working — clean tree by Phase 5).
- Round counter under limit
- Diff classification: behavioral — PUT `/api/settings` now invokes `enforceRateLimit` against the shared `TAGS_MUTATION` bucket so a client throttled at POST `/api/tags` cannot drive the same hashtag mutation through the settings endpoint to bypass the per-user cap. Companion tests pin the 429 contract in `tests/settings/api.test.ts` (CF-028) and `tests/settings/delete-initial.test.ts`.

## Phase 1 — Sync

No spec edit needed. The behavior shipped in `7e6f3ea` is already covered verbatim by REQ-AUTH-001 AC 9:

> Application-layer rate limits apply to every `/api/auth/*` route and **every authenticated mutation route**, keyed by IP for unauthenticated paths and by user id for authenticated mutation paths. Exhausted limits return HTTP 429 with a `Retry-After` header; the limiter fails open on the storage layer so a backing-store outage cannot lock users out of sign-in.

PUT `/api/settings` is an authenticated mutation route. Wiring rate-limiting there is a **conformance fix** that brings code into compliance with an already-correct spec — not a new contract. No AC update, no new REQ, no changelog entry warranted (per spec-discipline: no changelog for "verification pass after commit XXX" entries).

## Phase 2 — Validate

- **Forbidden content** in changed REQs (REQ-AUTH-001 AC 8/9 from the in-branch additions): `Cf-Access-Jwt-Assertion` (vendor protocol header — allowlisted), `429`/`Retry-After` (HTTP error contract — allowlisted for API contract REQs), `ADMIN_EMAIL` (env var that IS the contract — allowed contextually per spec-discipline allowlist), `/api/admin/*` and `/api/auth/*` route classes (these describe the *contract surface*, analogous to the env-var-as-contract carve-out — the route prefix IS the rule's scope). No rewrite needed.
- **REQ length:** REQ-AUTH-001 = 24 lines, REQ-PIPE-006 = 22 lines, REQ-PIPE-008 = 24 lines — all under the 25-line warn threshold.
- **Status discipline:** all changed REQs use one-word Status. OK.
- **Auto-demote (5a):** REQ-AUTH-001 has 10 test files referencing it; REQ-PIPE-006/008 each have 6+ test files. No demotion.
- **Source-vs-test (5b):** REQ-AUTH-001 has source annotations in `auth/[provider]/login.ts`, `auth/[provider]/callback.ts`, `admin/discovery/retry.ts`, `admin/force-refresh.ts`. Tests reference the REQ ID across `tests/auth/`, `tests/middleware/`, `tests/lib/`. AC 9's PUT /api/settings + star + tags surfaces are covered by tests — but those test files do NOT name REQ-AUTH-001 in their bodies (only `tests/settings/delete-initial.test.ts` does). See Phase 3 escalation.
- **Test quality (5c):** REQ-AUTH-001 has 9 ACs and well over 9 test functions. Count check passes. No tautology patterns (`expect(true).toBe(true)`), no empty bodies, no skipped tests on the relevant files (verified `grep` of `.skip|xit|describe.skip` returns empty).
- **Format compliance:** all required fields present on every changed REQ.
- **Cross-references:** REQ-AUTH-006/REQ-MAIL-003/REQ-PWA-002 references in `sdd/changes.md` resolve to "Out of Scope" entries in `sdd/README.md` (correct — historical changelog mentions of Out-of-Scope REQs are fine). REQ-OPS-005 reference in `.review-needed.md` is a *proposed* ID, not a broken cross-ref.
- **No duplicate REQs.**

## Phase 3 — Apply

- **No spec file edits this run** (the spec already correctly described what the code now enforces — this push closed the implementation gap, not a spec gap).
- 2 findings escalated to `sdd/.review-needed.md` (both out of lane for spec-reviewer):
  1. **MEDIUM** — 5 source files now enforce REQ-AUTH-001 AC 9 rate limits but do not carry the `Implements REQ-AUTH-001` annotation: `src/pages/api/articles/[id]/star.ts`, `src/pages/api/settings.ts`, `src/pages/api/tags.ts`, `src/pages/api/tags/delete-initial.ts`, `src/pages/api/tags/restore.ts`. Without the annotation, future source-vs-test grep cannot detect drift on these surfaces.
  2. **MEDIUM** — 2 new rate-limit test cases (`tests/settings/api.test.ts` "CF-028: returns 429…", `tests/articles/star.test.ts` 429 case) pin REQ-AUTH-001 AC 9 behavior but do not name REQ-AUTH-001 in their test titles or comments. Substring grep for REQ-AUTH-001 misses them.
- Both belong to the developer / code-reviewer / tdd-guide lanes, not spec-reviewer. spec-reviewer never edits source or test files.

## Phase 4 — Changelog

No changelog entry. Per spec-discipline:

> When NOT to add a changelog entry: … any change that doesn't affect what the product does.

The push is conformance-only against an already-correct spec. The 2026-04-27 entry "REQ-AUTH-001 hardens admin endpoints … Per-route application-layer rate limiting is also added on `/api/auth/*`" already announced AC 9 to users; restating it for an internal coverage extension would be noise.

## Phase 5 — Report

| Severity | Count | Notes |
|---|---|---|
| CRITICAL | 0 | — |
| HIGH | 0 | — |
| MEDIUM | 2 | Both out-of-lane (source/test annotations); escalated |
| LOW | 0 | — |
| Auto-fixed | 0 | Spec already correct — no edit needed |
| Escalated | 2 | Source annotation gap + test naming gap |

Round counter: 1/3 — counter reset, no spiral risk.

No commits made by spec-reviewer this run. The branch's spec edits from earlier rounds remain unchanged.
