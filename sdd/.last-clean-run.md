# spec-reviewer run — 2026-04-27 (round 4)

**Mode:** unleashed
**Trigger commit:** 42ad1e2 (fix(test): remove orphan assertUrlSafe describe block — fixup batch including the `REQ-DATA-001` → `REQ-PIPE-002` annotation rename in `src/lib/db.ts`)
**Branch:** review-2026-04-27/mechanical-cleanup (not main/master — unleashed permitted)
**Round counter:** 1/3 (last 3 commits: `42ad1e2 fix(test):` (user), `8003e70 [unleashed] [spec-reviewer] chore:` (agent), `b7e7630 refactor:` (user) — only 1/3 agent-authored, well under the 2/3 spiral limit)

## Phase 0 — Triage

- SDD bootstrap detected (`sdd/README.md` present)
- Config: mode=unleashed, enforce_tdd=true (forced)
- Working tree clean
- User overrides parsed: 3 entries (REQ-DISC-002, REQ-SET-004 deprecated-with-live-code; REQ-MAIL-001 column-name allowance)
- Round counter under limit
- Diff classification: **behavioral over the whole branch, but every behavioral surface in this push was already covered by spec edits in earlier rounds of the same branch.** This run's incremental delta against round-3 is the orphan-annotation fixup commit (42ad1e2) — non-behavioral from the spec's point of view; it's a code-side resolution of a previously-escalated finding.

## Phase 1 — Sync

No spec edit needed. The behavioral content in the branch — REQ-AUTH-001 AC 8/9 (admin three-layer gate + per-route rate limits), REQ-AUTH-002 AC 4 (5-minute silent-refresh threshold extended across page navigation), REQ-PIPE-006 AC 6/7 (finish-time idempotency + per-tick counter idempotency under redelivery), REQ-PIPE-008 AC 9 (no-double-finalize and no-stranded-finalize contract including the rollback-failure sub-case) — is already in `sdd/authentication.md` and `sdd/generation.md`, written verbatim by earlier `[unleashed] [spec-reviewer]` rounds on this branch. The 42ad1e2 fixup commit fixed source-side annotation orphans the user picked up from the prior round-3 escalation (`src/lib/db.ts` line 1 now reads `// Implements REQ-PIPE-002`); spec-reviewer never wrote that annotation, so the fix is a code-lane resolution of a code-lane escalation — not a spec sync.

## Phase 2 — Validate

- **Forbidden content** in changed REQs (REQ-AUTH-001 AC 8/9, REQ-PIPE-006 AC 6/7, REQ-PIPE-008 AC 9, REQ-AUTH-002 AC 4): `Cf-Access-Jwt-Assertion` (vendor protocol header — allowlisted), `429`/`Retry-After` (HTTP error contract — allowlisted), `ADMIN_EMAIL` (env var that IS the contract — contextual carve-out), `/api/admin/*` and `/api/auth/*` route classes (the route prefix IS the rule's scope — analogous carve-out), `Workers AI` (vendor — allowlisted). No rewrite needed.
- **REQ length:** REQ-AUTH-001 = 24 lines, REQ-AUTH-002 = 12 lines, REQ-PIPE-006 = 22 lines, REQ-PIPE-008 = 24 lines — all under the 25-line warn threshold.
- **Status discipline:** all changed REQs use one-word Status (`Implemented`). OK.
- **Auto-demote (5a):** REQ-AUTH-001 has 11 test files referencing it; REQ-AUTH-002 has 3+; REQ-PIPE-006 has 2 dedicated test files (`tests/pipeline/scrape-status.test.ts` 8 hits, `tests/pipeline/scrape-run.test.ts` 7 hits); REQ-PIPE-008 has 2 dedicated test files (`tests/pipeline/finalize-consumer.test.ts`, `tests/pipeline/finalize-merge.test.ts`). No demotion.
- **Source-vs-test (5b):** `src/lib/db.ts` annotation now correctly points at REQ-PIPE-002 (orphan resolved by 42ad1e2). Five source files still lack the `REQ-AUTH-001` annotation despite enforcing AC 9 — pre-existing escalation, kept open. Two test files still don't name `REQ-AUTH-001` despite pinning AC 9 — pre-existing escalation, kept open. All three pre-existing escalations remain in `.review-needed.md`.
- **Test quality (5c):** REQ-AUTH-001 has 9 ACs and 11+ test files; REQ-PIPE-006 has 7 ACs and 15+ tests across the two files; REQ-PIPE-008 has 9 ACs and 10+ tests. All AC-count vs test-count checks pass. Repo-wide grep for tautology patterns (`expect(true).toBe(true)`, `assertTrue(True)`, empty `it(...)` bodies) returns 0 hits. Repo-wide grep for skipped tests (`.skip`, `xit`, `xdescribe`, `@pytest.mark.skip`, `t.Skip()`) returns 0 hits.
- **Format compliance:** all required fields present on every changed REQ.
- **Cross-references:** REQ-AUTH-007 / REQ-DISC-006 / REQ-MAIL-001 / REQ-PWA-001 etc. all resolve. REQ-OPS-005 reference in `.review-needed.md` is a *proposed* ID, not a broken cross-ref.
- **No duplicate REQs.**

## Phase 3 — Apply

**One spec edit this run:**

- `sdd/.review-needed.md` cleaned. The "MEDIUM: `src/lib/db.ts` has orphan annotation `Implements REQ-DATA-001`" entry was removed because the user resolved it in 42ad1e2 (line 1 of `src/lib/db.ts` now reads `Implements REQ-PIPE-002`, repo-wide grep for REQ-DATA-001 returns only `.review-needed.md` self-references). The 2026-04-25 force-refresh entry was updated to reflect the latest annotation set on the file (`REQ-PIPE-001` + `REQ-OPS-004` + `REQ-AUTH-001`, the last of which was correctly added in the rate-limit branch).

**Three pre-existing escalations carried forward unchanged:**

1. **MEDIUM** — Five source files (`src/pages/api/articles/[id]/star.ts`, `src/pages/api/settings.ts`, `src/pages/api/tags.ts`, `src/pages/api/tags/delete-initial.ts`, `src/pages/api/tags/restore.ts`) enforce REQ-AUTH-001 AC 9 rate limits but lack the `Implements REQ-AUTH-001` annotation. Source code is out-of-lane for spec-reviewer.
2. **MEDIUM** — Two test files (`tests/settings/api.test.ts`, `tests/articles/star.test.ts`) pin REQ-AUTH-001 AC 9 behaviour but their test titles and file bodies do not name REQ-AUTH-001. Test code is out-of-lane for spec-reviewer.
3. **MEDIUM** — `tests/pipeline/chunk-consumer.test.ts` test "REQ-PIPE-002: addChunkStats is gated by completion INSERT — no double-count under redelivery" pins REQ-PIPE-006 AC 7 contract but is named after REQ-PIPE-002. Test code is out-of-lane for spec-reviewer.

**One pre-existing HIGH carried forward (refined):**

4. **HIGH** — `src/pages/api/admin/force-refresh.ts` has no dedicated REQ. The route's contract (operator-only, content-negotiated 303-vs-JSON, REUSE_WINDOW_SECONDS guard, three-layer admin gate) cuts across observability and generation domains. Cross-domain placement is a JUDGMENT call; conservative unleashed rule says don't invent a REQ where domain placement is ambiguous. Three resolution options remain on the table.

## Phase 4 — Changelog

No changelog entry. Per spec-discipline:

> When NOT to add a changelog entry: … strikethrough cleanup, status field truncation, format fixes, implementation leakage moved to docs, any change that doesn't affect what the product does.

Removing a stale review-queue entry that the user already resolved in code is review-queue maintenance, not a behavioral observation. The behavioral content of this branch was already announced in the 2026-04-27 changelog block (admin endpoint hardening, silent-refresh threshold, finalize-lock and chunk-counter idempotency); restating it for an internal review-queue tidy would be exactly the "verification pass after commit XXX" entry the discipline rule explicitly forbids.

## Phase 5 — Report

| Severity | Count | Notes |
|---|---|---|
| CRITICAL | 0 | — |
| HIGH | 1 | force-refresh REQ gap (carried forward, refined to reflect latest annotation set) |
| MEDIUM | 3 | 5 source-file annotation gap, 2 test-file naming gap, 1 chunk-consumer test naming gap (all carried forward, all out-of-lane for spec-reviewer) |
| LOW | 0 | — |
| Auto-fixed | 1 | Cleared resolved entry from `.review-needed.md` (`src/lib/db.ts` orphan annotation, fixed by user in 42ad1e2) |
| Escalated | 4 | All pre-existing — 3 MEDIUM + 1 HIGH carried forward |

Round counter: 1/3 — well under the spiral limit. The branch's spec edits from earlier rounds remain unchanged.

No new commits made by spec-reviewer this run beyond the `.review-needed.md` and `.last-clean-run.md` edits.
