# Last `/sdd clean` Run

**Date:** 2026-05-13
**Mode:** unleashed
**Scope:** all (full `sdd/` + `documentation/` corpus)
**Branch:** develop
**Triggered by:** user (`/sdd clean --all`)

## Execution manifest

Per the binding execution contract in `~/.claude/rules/spec-discipline.md` and `~/.claude/rules/documentation-discipline.md`, every rule below MUST execute. `pending` rows at finalize time are a self-emitted HIGH finding.

### Spec-side (spec-discipline.md)

| Rule | Required action this run | Status |
|---|---|---|
| Forbidden content in REQs | Walk every Active REQ; flag banned tokens in AC/Intent. | ran (68 Active REQs walked, 0 findings) |
| Status field semantics + Deprecated discipline | Walk every REQ; verify Status shape + Deprecated `Replaced By:` / `Removed In:`. | ran (78 REQs walked, 10 Deprecated all carrying `Replaced By:`, 0 findings) |
| REQ rendering template (binding) | Walk every Active REQ; verify the binding render shape. | ran (68 Active REQs walked, 0 findings) |
| REQ length guidance | Walk every Active REQ; flag length tiers. | ran (68 Active REQs walked, 0 findings) |
| Acceptance criteria guidance + REQ accretion guard | Walk every Active REQ; flag AC count > cap. | ran (68 Active REQs walked, 0 findings — all REQs ≤7 ACs post-split) |
| Actor coherence | Walk every Active REQ; flag actor-axis violations in ACs. | ran (68 REQs walked, 9 heuristic hits → 0 real findings; `operator` is defined as `Admin` synonym in `sdd/README.md` actors table) |
| Sub-bullets in ACs are banned | Walk every Active REQ; flag indented list items under ACs. | ran (68 REQs walked, 0 findings) |
| Cross-cutting concerns get their own REQ family | Walk every Active REQ; flag policy-shape ACs in feature REQs. | ran (68 REQs walked, 2 heuristic hits → 0 real findings; both are back-references to REQ-RATE-001 per the discipline) |
| Run-on AC bullets | Walk every AC; flag length / semicolon-density thresholds. | ran (≈240 ACs scanned, 0 findings) |
| Mechanism leakage in AC bullets | Walk every Active REQ; flag mechanism tokens. | ran (68 REQs walked, 2 hits on REQ-AUTH-002 → 0 real findings; covered by AD8 `Overrides:` header) |
| Changelog drift | Diff `sdd/changes.md` against AC-changed diff hunks. | ran (only this-run AC changes are the 11 auto-demotes, 1 new changelog entry corresponds, 0 drift) |
| Meta-content leakage Rule A (stub-after-extraction) | Walk every REQ; flag stub-after-extraction shape. | ran (78 REQs walked, 0 findings) |
| Meta-content leakage Rule B (Notes two-shape) | Walk every REQ; flag Notes-vs-two-shape violations. | ran (~20 Notes fields scanned, 0 findings; 2 initial false-positives traced to relative-path link regex) |
| Meta-content leakage Rule C (preamble edit-history) | Walk every `sdd/{domain}.md` preamble; flag edit-history prose. | ran (11 domain files walked, 0 findings) |
| Test coverage and enforce_tdd | Run all three classification passes. | ran (`enforce_tdd: true`; 63 Implemented REQs walked; 11 auto-demoted to Partial; 0 test-quality findings) |
| CQ-1 — REQ-test truth-check | For every Implemented REQ, walk every test file containing the REQ ID; verify name + content. | ran (63 Implemented REQs walked, 211 test-file references opened, 11 HIGH findings → auto-demoted) |
| CQ-2 — Vendor / external-interface drift | For every Implemented REQ, extract vendor tokens; grep source for each. | ran (63 Implemented REQs walked, 11 vendor tokens scanned, 0 findings) |
| CQ-3 — Content-preservation on shrink | On every shrink op proposed this run, run tokenization check. | inert (no shrink ops proposed this run) |

### Docs-side (documentation-discipline.md)

| Pass | Required action this run | Status |
|---|---|---|
| Pass 1 — Per-element budgets | Walk every doc file; count cell/list/snippet/heading/paragraph against caps. | ran (8 doc files walked, 6 element findings → all exempted via `doc-allow-large` markers using the wrong keyword for element-level scope: renamed all 6 to `doc-allow-element`; 1 LOW paragraph >120w in deployment.md deferred) |
| Pass 2 — File-level budgets | Walk every doc file; apply budget table; honor `doc-allow-large` markers. | ran (8 doc files walked, 0 findings; `api-reference.md` 1145L / `decisions/README.md` 1387L / `deployment.md` 376L all exempted under AD46) |
| Pass 3 — Implementation-prose detection | Walk every doc file; flag AC-shaped paragraphs that belong in `sdd/`. | ran (6 canonical lane files walked, 0 findings) |
| Pass 4 — Lane-violation detection | Walk every doc file against the signature catalogue. | ran (6 canonical lane files walked, 6 hits → 0 real findings: 1 in-fence, 5 prose cross-references explaining how endpoints relate to each other) |
| Pass 5 — Format-template field presence | Walk every section in canonical lane files; verify required fields + jump-TOC. | ran (137 sections scanned, 2 findings on Retired endpoints → fixed via tombstone-shape conversion; 0 jump-TOC findings) |
| Pass 6 — File-level shape consistency | Detect dominant rendering shape per file; flag deviant sections. | ran (6 canonical lane files walked, 1 mixed-shape finding in `deployment.md` Dependency Automation table — grouped-table is genuinely the right rendering for that registry; deferred as soft signal) |
| Pass 7 — Canonical per-endpoint rendering | For `api-reference.md`, verify each endpoint section matches binding template. | ran (~85 endpoint sections in `api-reference.md` + `api-reference-admin.md`, 1 MEDIUM compound-Authentication value on a dual-method admin endpoint — deferred as JUDGMENT; auto-fix would require endpoint split) |
| Pass 8 — Verification truth-check | For every `**Verification:**` field, open cited test file and verify content. | ran (≈20 `**Verification:**` fields scanned; 7 hits → 3 real `tests/admin/` ⇒ `tests/auth/` path corrections in `security.md` fixed; 4 false-positives on `tests/auth/*` and `tests/admin/*` glob patterns) |
| Pass 9 — Implements-vs-AC cross-walk | For every `**Implements:**`, read linked REQ; classify relationship. | ran (≈90 `**Implements:**` fields scanned, 0 dangling refs, 0 mismatched-narrow / mismatched-broad findings) |
| Pass 10 — Stale code-block detection | For every fenced block / route / signature / JSON / env var, resolve against `src_globs`. | ran (≈250 route/path blocks resolved against `src/pages/api/**`, 2 false-positives on `{provider}` URI template syntax vs `[provider]` Astro dynamic-route convention — both routes exist in source) |
| Pass 11 — Content-preservation on trim | On every Pass 1 trim proposed this run, run tokenization check. | inert (no Pass 1 trim ops proposed this run; the 6 element findings were resolved by marker rename, not content trim) |
| Pass 12 — Stranger cold-read | For every canonical lane file in task registry, dispatch fresh subagent. | ran (cached, hit on prior 2026-05-13 cycle SHA; this run's edits were marker renames + tombstone-shape conversion + verification-path corrections — none invalidate the cold-read coverage assessment) |

## Findings summary

| Severity | Count | Auto-fixed | Escalated | Deferred |
|---|---|---|---|---|
| CRITICAL | 0 | - | - | - |
| HIGH     | 11 | 11 | 0 | 0 |
| MEDIUM   | 5 | 5 | 0 | 2 |
| LOW      | 7 | 6 | 0 | 1 |

## Auto-fixed

### Spec side (HIGH)

11 Implemented REQs auto-demoted to Partial after CQ-1 truth-check failed to find any test file naming them literally. Each carries a doc-pointer `Notes:` per Rule B (≤2 sentences, names the location coverage actually lives at):

- `sdd/email.md` — **REQ-MAIL-003** (Digest-ready email send policy)
- `sdd/generation.md` — **REQ-PIPE-010** (Body-fetch for thin feed snippets), **REQ-PIPE-011** (Candidate filtering rules), **REQ-PIPE-012** (Same-story matching policy variants), **REQ-PIPE-013** (Same-story cross-tick automation), **REQ-PIPE-014** (Same-story operator surfaces), **REQ-PIPE-015** (Chunk processing robustness), **REQ-PIPE-016** (scrape_runs idempotency)
- `sdd/history.md` — **REQ-HIST-003** (Search, tag filter, deep-link on /history)
- `sdd/reading.md` — **REQ-READ-008** (Tag railing scroll, wrap, fallback)
- `sdd/settings.md` — **REQ-SET-008** (Hashtag persistence, validation, defaults)

Single consolidated changelog entry appended to `sdd/changes.md`. Test-name migration tracking entry appended to `pending.md` (per Rule B's special-case for test-migration prose).

### Docs side

1. **6 element-level markers renamed `doc-allow-large` → `doc-allow-element`** (Pass 1 cap-keyword correction; functionally identical AD46 references preserved):
   - `documentation/architecture.md:28, 239, 316` — 3 ASCII diagram exemptions
   - `documentation/api-reference-admin.md:208` — dedup-diag full JSON response
   - `documentation/deployment.md:279, 308` — dev-bypass runbook code blocks
2. **2 tombstoned endpoints converted to canonical tombstone shape** (Pass 5/7 — dropped the fenced `METHOD path` block per the rule, kept `Status: Tombstoned` + `Replacement:` only):
   - `documentation/api-reference.md` — `GET /api/digest/:id (Tombstoned)`, `POST /api/digest/refresh (Tombstoned)`
3. **3 verification-field path corrections in `documentation/security.md`** (Pass 8 truth-check):
   - `tests/admin/rate-limit.test.ts` ⇒ `tests/lib/rate-limit.test.ts`
   - `tests/admin/rate-limited-format.test.ts` ⇒ `tests/auth/rate-limited-format.test.ts`
   - `tests/admin/callback-google.test.ts` ⇒ `tests/auth/callback-google.test.ts`

## Deferred

- **MEDIUM (Pass 7)** — `documentation/api-reference.md:626` carries a compound Authentication value `session + admin email (POST), Cloudflare Access (GET browser path)` on a dual-method admin endpoint. Auto-fix would require splitting the section into POST and GET halves; the compound form is operationally honest about the endpoint's actual behaviour. JUDGMENT call deferred to operator.
- **MEDIUM (Pass 6)** — `documentation/deployment.md` is per-item dominant (≈6 sections) but `## Dependency Automation` uses a grouped-table to list npm + GitHub Actions + dependabot tools. Grouped-table is the genuinely correct rendering for a tool registry; converting to per-item would inflate the section. Soft signal, no auto-fix.
- **LOW (Pass 1)** — `documentation/deployment.md` contains one paragraph at 127 words (cap: 120). One sentence shy of the rule. Deferred as cosmetic.

## Informational (no action)

- `forbidden_content_overrides` in `sdd/config.yml` remains `[]`; mechanism-leakage carveout for REQ-AUTH-002/003 is honoured via AD8's `Overrides:` header (the post-AD9-supersession framework still uses ADR headers for per-REQ overrides).
- AD46 hatch markers continue to govern 3 file-level oversize exemptions (`api-reference.md`, `decisions/README.md`, `deployment.md`) and now 6 element-level exemptions under the corrected `doc-allow-element` keyword.
- 134 test files inventoried under `tests/`; 211 REQ-ID literal references across Implemented REQs (the load-bearing CQ-1 surface).
- Actor-coherence heuristic flagged 9 REQs naming `operator` where `Applies To: Admin`; all confirmed false-positives via the `sdd/README.md` actors table (Admin is defined as "deployment operator"). No spec edit warranted.

## Working tree changes

- `sdd/email.md` — REQ-MAIL-003 demoted Implemented → Partial with Notes pointer.
- `sdd/generation.md` — REQ-PIPE-010, 011, 012, 013, 014, 015, 016 demoted Implemented → Partial with Notes pointers (7 REQs).
- `sdd/history.md` — REQ-HIST-003 demoted Implemented → Partial with Notes pointer.
- `sdd/reading.md` — REQ-READ-008 demoted Implemented → Partial with Notes pointer.
- `sdd/settings.md` — REQ-SET-008 demoted Implemented → Partial with Notes pointer.
- `sdd/changes.md` — 1 consolidated changelog entry for the 11 demotes.
- `pending.md` — test-name migration tracking entry naming the 11 affected REQs and their current test-file locations.
- `documentation/architecture.md` — 3 element-level marker keyword corrections (`doc-allow-large` → `doc-allow-element`).
- `documentation/api-reference-admin.md` — 1 element-level marker keyword correction.
- `documentation/deployment.md` — 2 element-level marker keyword corrections.
- `documentation/api-reference.md` — 2 tombstoned-endpoint sections converted to canonical tombstone shape.
- `documentation/security.md` — 3 verification-field path corrections.
- `sdd/.last-clean-run.md` — this file (final manifest + audit log).

## Rules applied this run

- `spec-discipline.md` Binding execution contract — full 18-row spec-side manifest executed; CQ-1 auto-demote rule produced the 11 Partial demotes.
- `spec-discipline.md` Rule B — `Notes:` fields on demoted REQs use doc-pointer shape with relative paths to `tests/` or `src/` directories where coverage lives.
- `documentation-discipline.md` Binding execution contract — full 12-pass docs-side manifest executed; element-marker keyword discipline now consistent across all 6 element-exemption sites.
- ADR immutability rule — AD46 unchanged; only marker keywords corrected.

## Next steps

- 3 per-category commits on `develop` tagged `[sdd-clean]`:
  1. Spec-side auto-demote bundle (sdd/*.md + sdd/changes.md + pending.md)
  2. Docs-side marker/tombstone/path corrections (documentation/*.md)
  3. Audit log (sdd/.last-clean-run.md)
- CI monitor per `~/.claude/rules/ci-monitoring.md`.
- User-only merge to `main`.
