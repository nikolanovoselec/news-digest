# Last spec-reviewer run

**Date:** 2026-04-29 (third pass, post-user-edit)
**Mode:** unleashed
**Branch:** review-2026-04-29/security-and-critical
**Round counter:** 1 of 2 (last 3 commits = `c582738c spec(ops): broaden REQ-OPS-004 AC 6 to per-character <>& escape` user, `f30d276 fix(review): code-reviewer v5 highs` non-tagged, `f2cb7a1 [unleashed] [spec-reviewer] chore(spec): refresh last-clean-run audit log` agent — only 1 carries the `[spec-reviewer]` tag, counter under threshold)

## Summary

| Severity | Count | Notes |
|---|---|---|
| CRITICAL | 0 | — |
| HIGH | 0 | — |
| MEDIUM | 1 | REQ-SET-006 AC 5 + REQ-SET-007 AC 7 lack dedicated 429-on-exhaustion tests — escalated to `.review-needed.md` |
| LOW | 0 | — |
| Auto-fixed | 1 | Tightened the 2026-04-29 changelog entry for REQ-OPS-004 AC 6 to match the broadened AC the user landed in `c582738c` |
| Escalated | 1 | The 429-test gap above |

## Phase 0 (triage)

- SDD bootstrap present (`sdd/` + `sdd/README.md`).
- `sdd/config.yml` read: `mode: unleashed`, `enforce_tdd: true`.
- Round counter: 1 of 2. Last 3 commit subjects checked; only `f2cb7a1` carries the `[spec-reviewer]` tag. The user commit `c582738c` is the manual edit predicted in the previous run's `.review-needed.md` ("Resume" section), and it both resets the spiral and resolves the AC-6 prose drift.
- `.user-overrides.md` loaded: 2 entries (`forbidden-content-column-name:REQ-MAIL-001`, `skipped-test:REQ-READ-002,REQ-HIST-001`). Neither matched any finding this run.
- Diff classification (`origin/main...HEAD`): behavioural — source code (security headers, JSON-LD helper, email-html helper, admin-auth warning, two new rate-limit rules), test additions (`tests/lib/json-ld.test.ts`, the CSP-enforcement Playwright block, the KV stubs in `set-tz.test.ts` and `discovery-status.test.ts`), workflow tightening (npm audit gate moved from advisory-only to CRITICAL-blocking and replicated in `deploy.yml`). Continued into Phase 1.
- Working tree clean before edits. Branch is not main/master.

## Phase 1 (sync)

The bulk of the spec sync was already completed by the prior runs `3c3ab4f`, `24d6d3a`, `b74cb1c`, and the user's manual `c582738c`. This run found one remaining drift:

- **changes.md 2026-04-29 entry for REQ-OPS-004 AC 6 understated the broadened AC.** AC 6 now reads "rewrites every `<`, `>`, and `&` byte to its `\uNNNN` JSON form, defeating every HTML state-transition vector that could escape the script block (`</script>`, `<!--`, `]]>`, `<script` re-entry)" but the changelog entry written in `b74cb1c` still described only the narrower `</script>` case-insensitive sweep. **Action**: rewrote the changelog bullet to match the broadened AC text (≤2 sentences, user-facing, dated entry preserved).

No new REQs needed. Behavioural deltas without their own REQ:

- `src/lib/email-html.ts` — pure refactor extracting `escapeHtml` + `headlineRow` from `src/lib/email.ts`. No behaviour change; the email render path's HTML-escape contract is in REQ-MAIL-001 unchanged.
- `src/pages/api/digest/[id].ts` collapsed to `410 Gone` — already covered by the existing 2026-04-29 entry "Per-digest retrieval endpoint retired".
- `src/pages/history.astro` — `innerHTML = ''` swapped for `replaceChildren()`. Same user-visible behaviour.
- `src/scripts/theme-toggle.ts` — `theme` cookie now carries `Secure`. Implementation-level cookie hardening; REQ-DES-002 AC 2 already requires "persists the choice for the current browser" without specifying flags.
- `src/styles/global.css` — `html[data-theme]` background pinned to literal hex (replaces the `style=` attribute that CSP `style-src 'self'` now rejects). Same first-paint invariant as REQ-OPS-004 AC 5.
- `src/components/DigestCard.astro` — removed inline `style="--stagger-delay: …"`. The stagger animation lived in the now-Deprecated REQ-READ-004; the component still emits `data-stagger-index`.
- `.github/workflows/test.yml` + `deploy.yml` — npm audit production-deps gate moved from advisory-only HIGH+ to blocking CRITICAL+, and replicated as a defence-in-depth step in `deploy.yml` between the merge gate and the deployed Worker. Operational/CI-internal — belongs in `documentation/`, not in `sdd/` per spec discipline.
- `.dev.vars.example` — added `ADMIN_EMAIL` and `CF_ACCESS_AUD` documentation. Configuration documentation — `documentation/configuration.md`'s lane.

## Phase 2 (validate)

- **Forbidden content** — clean. AC 6's `</script>`, `<!--`, `]]>`, `<script` are user-facing HTML-state tokens (allowed under the user-facing-strings allowlist; they ARE the contract). No new hex codes, file paths, function names, env vars, or HTTP status codes leaked into REQs.
- **REQ length** — every touched REQ under the soft warn threshold or with structured AC sub-bullets that preserve intent.
- **Status field discipline** — every Status value across the spec is one word from {Implemented, Deprecated}.
- **Fake-Deprecated** — every `Deprecated` REQ carries both `Replaced By:` and `Removed In:` (REQ-GEN-001/002/003/004/005, REQ-READ-004, REQ-AUTH-007 supersession).
- **Test coverage 5a (auto-demote)** — every `Implemented` REQ touched on the branch has at least one test referencing the REQ ID. No demote needed. (REQ-OPS-003: 17 tests; REQ-OPS-004: 23 tests; REQ-AUTH-001: 74 tests; REQ-AUTH-005: 1 test file; REQ-PIPE-001: 7 test files; REQ-PIPE-008: 5 test files; REQ-SET-006: 17 tests; REQ-SET-007: 19 tests; REQ-MAIL-001: 8 test files.)
- **Source-vs-test coverage 5b** — every REQ with source on this branch also has tests. No `Planned` → `Partial` promotions needed.
- **Test quality 5c** — AC count vs test count: every REQ has test_count >> ac_count (ratio always >= 2.7). No tautological assertions in the touched test files; `expect(body.ok).toBe(true)` is asserting against a response field, not a literal `true === true`. The three `test.skip(...)` instances in `view-transition.spec.ts` are the perf-comparability skip (silenced by user override), the storage-state environmental gate (line 35), and a runtime data guard (line 153) — none flagged.
- **One MEDIUM heuristic finding remains** — REQ-SET-006 AC 5 and REQ-SET-007 AC 7 each declare a 429-with-`Retry-After` contract on bucket exhaustion, but neither test file asserts on the 429 response after pre-seeding the bucket past the limit. Escalated to `.review-needed.md` because writing tests is `tdd-guide`'s lane.
- **Format compliance** — every REQ has all required fields.
- **Cross-references** — all `REQ-*` refs resolve. The "(was REQ-X-NNN)" entries in `sdd/README.md` Out of Scope are decision-history annotations, not broken refs.
- **Constraint refs** — all 9 `CON-*` refs resolve to definitions in `sdd/constraints.md`.
- **Domain consistency** — every domain listed in `sdd/README.md` has a file.
- **Strikethrough / dual-branch ACs** — none present.

## Phase 3 (apply)

Two edits applied directly on the current branch (`review-2026-04-29/security-and-critical`):

1. `sdd/changes.md` — tightened the 2026-04-29 REQ-OPS-004 AC 6 bullet to match the broadened AC text.
2. `sdd/.review-needed.md` — cleared the stale round-limit diagnostic, replaced with the concise REQ-SET-006/007 429-test follow-up.

Plus this audit log update at `sdd/.last-clean-run.md`.

Single per-category commit: `[unleashed] [spec-reviewer] sync(spec): align changelog with broadened OPS-004 AC 6`.

## Phase 4 (changelog)

No new dated entry. The single Phase 1 edit refines an existing 2026-04-29 entry to match an AC the user themselves broadened — same calendar day, same conceptual change, no need for a fresh entry.

## Phase 5 (report)

```
spec-reviewer report — mode: unleashed
  CRITICAL: 0
  HIGH:     0
  MEDIUM:   1 (REQ-SET-006 AC 5 + REQ-SET-007 AC 7 missing 429 test — escalated)
  LOW:      0
  Auto-fixed: 1 (changelog text aligned with broadened AC)
  Escalated to .review-needed.md: 1 (the MEDIUM above)
  Round counter: 1 of 2
```

## Notes / followups

- Open: REQ-SET-006 AC 5 / REQ-SET-007 AC 7 dedicated 429 tests. See `.review-needed.md`.
- The npm audit CI gate change (HIGH advisory-only → CRITICAL blocking, replicated to deploy.yml) and the new `ADMIN_EMAIL` / `CF_ACCESS_AUD` lines in `.dev.vars.example` are operational/configuration documentation. They sit in doc-updater's lane, not spec-reviewer's; spec discipline keeps build-tool internals and env-var enumerations out of REQs.
