# Last spec-reviewer run

**Date:** 2026-04-29
**Mode:** unleashed
**Branch:** review-2026-04-29/security-and-critical
**Round counter:** 0 (first agent commit on this branch)

## Summary

| Severity | Count | Notes |
|---|---|---|
| CRITICAL | 0 | — |
| HIGH | 0 | — |
| MEDIUM | 0 | — |
| LOW | 0 | — |
| Auto-fixed | 1 | Added REQ-OPS-004 AC 6 (JSON-LD defensive escape) |
| Escalated | 0 | — |

## Phase 0 (triage)

- SDD bootstrap present (`sdd/` + `sdd/README.md`).
- `sdd/config.yml` read: `mode: unleashed`, `enforce_tdd: true`.
- Round counter: 0 — last 3 commits did not contain `[autonomous]`,
  `[unleashed]`, or `[spec-reviewer]` markers.
- `.user-overrides.md` loaded: 2 entries (forbidden-content-column-name
  for REQ-MAIL-001, skipped-test for REQ-READ-002/REQ-HIST-001 perf
  test). Neither matched any finding this run.
- Diff classification: behavioural (source code, schema-adjacent,
  workflow). Continued into Phase 1.
- Working tree clean before edits. Branch not main/master.

## Phase 1 (sync)

Behavioural changes already reflected in `sdd/` on the branch before
this run:

- REQ-OPS-003 AC 1 — CSP `style-src` tightened from
  `'self' 'unsafe-inline'` to `'self'`.
- REQ-AUTH-001 AC 8 split into 8a–8c with the AUD-unset operational
  warning logged structurally.
- REQ-AUTH-001 AC 9 split into 9a–9f, broadening rate-limit coverage to
  authenticated polling endpoints and naming the fail-mode split.
- REQ-AUTH-005 AC 1 split into 1a–1c (device fingerprint policy).
- REQ-PIPE-001 AC 8 split into 8a–8e (SSRF-guarded body fetch).
- REQ-PIPE-008 AC 4–9 reshaped (idempotency clarifications).
- REQ-SET-006 AC 5 added (discovery-progress polling rate-limit).
- REQ-SET-007 AC 7 added (timezone-update rate-limit).
- REQ-GEN-001/002/004/005 in `Out of Scope` now carry `Replaced By` +
  `Removed In` fields (no longer fake-Deprecated).
- README.md "Security by construction" line and "Out of Scope" line
  updated to match the new SSRF-guarded fetch posture.

One drift remained when this run started:

- Three new tests (`tests/lib/json-ld.test.ts:17/22/28`) and the new
  `src/lib/json-ld.ts` helper reference `REQ-OPS-004` for the
  defensive `</script>` escape, but no AC covered it. The existing
  REQ-OPS-004 only documented title/description/canonical/OG metadata,
  the crawler policy, the agents file, the sitemap, and no-index error
  pages.

Action: added REQ-OPS-004 AC 6 for the JSON-LD defensive serializer.
Added changelog entry to `sdd/changes.md`.

## Phase 2 (validate)

- **Forbidden content** — clean. The new AC 6 contains `</script>`
  inside backticks (a security-contract token, allowed under the
  user-facing-strings allowlist).
- **REQ length** — REQ-AUTH-001 33 lines (LOW, structured AC bullets
  preserved as intent, not implementation leakage). REQ-PIPE-001 27,
  REQ-PIPE-008 27, REQ-AUTH-005 18. None auto-shrunk: every long bullet
  is observable contract.
- **Status field** — every Status touched is one word. ✓
- **Fake-Deprecated** — every `Deprecated` REQ has `Replaced By:` +
  `Removed In:` (REQ-GEN-001/002/004/005, REQ-READ-004). ✓
- **Test coverage (5a auto-demote)** — every `Implemented` REQ touched
  on the branch has at least one test referencing the REQ ID. No
  demote needed.
- **Source-vs-test coverage (5b)** — REQ-OPS-004 has source
  (`json-ld.ts`, `Base.astro`, `sitemap.xml.ts`, …) and tests
  (`crawler-policy.test.ts`, `json-ld.test.ts`). REQ-OPS-003 has source
  (`security-headers.ts`) and tests (`security-headers.test.ts`,
  `view-transition.spec.ts` CSP enforcement block). REQ-SET-006/007
  source has matching test files. No gap.
- **Test quality (5c)** — `json-ld.test.ts` has 3 tests for AC 6
  (covers the `</script>` rewrite invariant from both directions). No
  tautology patterns, no skipped tests added.
- **Format compliance** — every REQ has all required fields. ✓
- **Cross-references** — all `REQ-*` refs in modified files resolve to
  existing headers (out-of-scope `(was REQ-AUTH-006)` style refs are
  decision-history, not broken refs).
- **Constraint refs** — no broken refs. CON-COST-001 / CON-TECH-001
  defined-but-unused (preexisting, not introduced this run).
- **Domain consistency** — every domain in README.md has a file. ✓
- **Strikethrough / dual-branch ACs** — none present. ✓

## Phase 3 (apply)

Single edit applied directly on the current branch
(`review-2026-04-29/security-and-critical`):

1. `sdd/observability.md` — add REQ-OPS-004 AC 6 (JSON-LD defensive
   escape).
2. `sdd/changes.md` — add a 2026-04-29 changelog entry for the new AC.

Committed as a single category: `[unleashed] [spec-reviewer]
sync(spec): json_ld_defence`. Pushed to origin.

## Phase 4 (changelog)

One entry added on top of the 4 already present for 2026-04-29.

## Notes / followups

- None outstanding. `sdd/.review-needed.md` left in its previous state
  ("No outstanding findings").
