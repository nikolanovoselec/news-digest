# Last spec-reviewer run

**Date:** 2026-05-06
**Mode:** unleashed (PR-boundary trigger on PR #201, develop branch, HEAD f0d903e)
**Branch:** develop (no main/master push; develop → main PR is unaffected)

## Findings

Single MEDIUM finding, auto-resolved:

- **MEDIUM — Run-on AC in REQ-PIPE-008 AC 1**: f0d903e appended a studies/audits/benchmarks non-merge clause to AC 1, pushing it from 107 → 153 words (over the 150-word ceiling).

## Resolution

Conservative fix per the unleashed JUDGMENT rule (preserve information, never overwrite intent, never renumber widely-referenced ACs):

- **AC 1 reverted to its pre-f0d903e wording** (107 words, dedup-pass + skip-when-≤1-article behavior only).
- **New AC 11 added** containing the studies/audits/benchmarks non-merge rule, with an explicit cross-reference back to AC 1 ("This rule overrides the same-content clustering described in AC 1 …").
- **AC 2-10 unchanged** so existing references in source files (`src/lib/finalize-merge.ts`, `src/queue/scrape-finalize-consumer.ts`, `src/lib/queue-handler.ts`, `src/lib/prompts.ts`), tests (`tests/pipeline/finalize-consumer.test.ts`, `tests/pipeline/finalize-merge.test.ts`, `tests/pipeline/chunk-consumer.test.ts`), the AD entry in `documentation/decisions/README.md` (AD around CF-026), and the changelog (3 historical lines mentioning REQ-PIPE-008 AC 5/AC 7/AC 9) all stay valid.

Why append rather than split mid-numbering: the alternative - promoting the studies clause to AC 2 and shifting AC 2-10 down - would have invalidated 14+ cross-references with no compensating benefit. The append-and-cross-reference pattern preserves both the AC's testability (the studies rule is now its own bullet) and the existing reference graph.

The studies/benchmarks rule itself has no test pinning it directly - REQ-PIPE-008 has 30+ tests in aggregate and the prompt-text contract that implements the rule lives at `src/lib/prompts.ts:293` - but spec-reviewer's heuristic 5c only flags ac_count > test_count, which REQ-PIPE-008 does not exhibit. A targeted test for AC 11 is recommended as a follow-up but is not auto-resolvable in this lane.

## Drift checks

- **Changelog updated**: the f0d903e entry "REQ-PIPE-008 AC 1 tightened" was re-titled "REQ-PIPE-008 AC 11 added" to match the new AC location. No drift introduced.
- **`.review-needed.md` resolution summary updated**: notes that the studies clause moved from AC 1 to AC 11 during the next-sweep run-on check, so future readers can follow the trail.

## Other passes (clean)

- Forbidden content: no new mechanism leakage, hex codes, file paths, or code snippets in the touched REQs.
- Cross-reference resolution: all REQ-* IDs in the diff resolve. Apparent "broken refs" in `sdd/README.md` (`REQ-AUTH-006`, `REQ-MAIL-003`, `REQ-PWA-002`) are intentional Out-of-Scope retrospective references - not active links.
- ADR table consistency: AD31 indexed in `documentation/decisions/README.md` table and matches its inline section. AD3-original is the superseded ADR (intentionally unindexed; dual-narrative-avoidance pattern done correctly).
- `Overrides:` headers parsed: AD8, AD9, AD23, AD25, AD29, AD30 - skip set built; no REQ-PIPE-* entries in scope.
- Round counter: 1 of 3 (last 3 commits = f0d903e [user], 0f59995 [doc-updater], 2a2253f [spec-reviewer]). Under limit.

## Status

`sdd/.review-needed.md` remains "no findings open" after this sweep. The single MEDIUM was auto-resolved without escalation.
