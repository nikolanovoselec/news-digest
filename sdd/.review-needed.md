# Review Needed

Findings escalated by spec-reviewer or doc-updater that require human attention. Resolve and clear entries as they are addressed.

---

## doc-vs-spec conflict: sdd/README.md Design Principle 7 + Out of Scope entry

**Raised by:** doc-updater  
**Severity:** HIGH  
**Date:** 2026-04-28

`sdd/README.md` contains two entries that directly contradict REQ-PIPE-001 AC 8 (`Status: Implemented`):

1. **Design Principle 7** (line 24):
   > "Security by construction — no inline scripts, **no server-side fetching of article bodies**, LLM output rendered as plaintext only"

2. **Out of Scope** entry (line 50):
   > "**Fetching article page bodies** — summaries are produced from search-API headlines and snippets; this eliminates an entire class of SSRF risk"

**Actual shipped behavior (per REQ-PIPE-001 AC 8 and AD3 revised 2026-04-27):** The chunk consumer _does_ fetch article HTML bodies for candidates whose feed snippet is below 400 characters, guarded by `src/lib/ssrf.ts`, an 8-second timeout, and a 1.5 MB cap.

**Required actions (spec-reviewer lane — do not touch from doc-updater):**

- Design Principle 7: remove the "no server-side fetching of article bodies" clause, or replace it with "SSRF-guarded article-body fetching with bounded timeout and size cap".
- Out of Scope entry: move "Fetching article page bodies" out of the Out of Scope list (since it is now in scope and implemented). Git history already records that it was once excluded.

**Not auto-resolved:** doc-vs-spec conflicts are never overwritten by either side per the spec-discipline rules.
