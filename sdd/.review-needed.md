# Review Needed

Findings escalated by spec-reviewer or doc-updater that require human attention. Resolve and clear entries as they are addressed.

## Notes

- Three earlier MEDIUM JUDGMENTs from the 2026-05-03 `/sdd clean` run are resolved:
  - **Cookie-attribute leakage** in REQ-AUTH-002 / REQ-AUTH-003 — recorded as **AD8: Cookie attributes are the security contract**.
  - **D1/KV shape leakage** in REQ-DISC-001/002, REQ-AUTH-002, REQ-SET-001/005 — recorded as **AD9: Storage-shape names are the persistence contract**.
  - **Architecture ASCII diagrams over the 15-line per-element cap** — annotated inline in `documentation/architecture.md` with `<!-- doc-allow-large -->` markers above each fenced diagram. Diagrams kept as-is.

- JUDGMENT resolutions go in ADRs (`documentation/decisions/`), not `sdd/.user-overrides.md`. The override file is deprecated by [codeflare#266](https://github.com/nikolanovoselec/codeflare/issues/266); spec-reviewer reads each ADR's `Overrides:` header to honour skips.

## 2026-05-06 — All previously escalated findings resolved

The five findings open before this sweep have been addressed:

- **MEDIUM (PR #201) — REQ-READ-001 AC 7 has no direct test coverage by REQ ID** → resolved. The existing wire-shape case in `tests/history/api.test.ts` was renamed to `REQ-HIST-001 / REQ-READ-001 AC 7: alt_source_count …` so spec-reviewer's REQ-grep counts it for both. A new pure-formatter helper at `src/lib/alt-source-label.ts` (`formatAltSourceLabel`) extracts the `+N` suffix and aria-label string production, with a 4-case unit test at `tests/lib/alt-source-label.test.ts` pinning the singular/plural aria-label and the no-suffix-on-zero contract. `DigestCard.astro` consumes the helper.
- **LOW (PR #201) — Stale REQ-PIPE-001 AC 2 wording vs MAX_CANDIDATES_PER_CHUNK = 25** → resolved. AC 2 rewritten to "partitions candidates across one or more chunk jobs whose size is capped to fit the model's context window," dropping the literal `~100` anchor. REQ-PIPE-002 Intent updated in the same vein ("batches sized to fit the model's context window").
- **MEDIUM (2026-05-05) — Two parallel Google News paths, ownership unclear** → resolved by **AD31** ("Google News baseline ownership lives at the coordinator, not in discovery"). Coordinator owns per-tag GN fan-out; discovery LLM's GN fallback is redundant during transition; follow-up issue tracks the prompt retrain.
- **MEDIUM (2026-05-05) — Per-item source-name labelling has no AC home** → resolved. REQ-PIPE-001 AC 4 extended with the per-item-publisher rule (Option A from the original recommendation: the label resolution lives with the source-list construction REQ, not on the reading surface).
- **MEDIUM (2026-05-05) — REQ-PIPE-008 AC 1 silent on the "different studies don't merge" rule** → resolved. The studies/audits/benchmarks non-merge rule is now its own bullet at REQ-PIPE-008 AC 11 (initially appended to AC 1, but spec-reviewer's run-on-AC check in the next sweep moved it to a standalone AC so existing AC 2-10 cross-references in source/tests/ADRs/changelog stay stable). Scope locked to studies/audits/benchmarks, not generalised to "any content with conflicting numbers" — that wider scope is left for a future pass if regressions show up beyond research papers.
- **LOW (2026-05-05) — `googleNewsSourceForTag` tests do not name a REQ ID** → resolved. The describe block was renamed to `googleNewsSourceForTag — REQ-PIPE-001 AC 9 auto-synthesised per-tag GN feeds`.

No findings open. The next spec-reviewer / doc-updater pass starts from a clean slate.

## 2026-05-06 — doc-updater PR #206 pass

**MEDIUM — table cell over 50-word cap (doc-discipline Pass 1)** — RESOLVED (PR #227)

`scrape-finalize-consumer.ts` role column trimmed to 36 words in this PR's architecture.md changes.

## 2026-05-07 -- doc-updater PR #215 pass: hard stop (anti-spiral)

All 3 of the last 3 commits are agent-authored ([doc-updater], [unleashed] [spec-reviewer] x2). Round counter limit reached (>=2 of last 3).

doc-updater did not run any validation or apply any fixes this pass. No findings were produced or suppressed.

**Action required:** make a non-agent commit (real code or manual edit) to reset the counter, then re-trigger the review pipeline if further doc validation is needed.

## 2026-05-07 — spec-reviewer PR #230 release pass — RESOLVED

All three findings from this pass are now addressed:

- **HIGH — REQ-PIPE-009 AC 5 per-invocation cap drift:** resolved in the same pass (AC 5 rewritten to describe the wall-clock-bounded behaviour; AD36 carries the rationale).
- **LOW — Changelog drift on the 2026-05-07 recalibration bullet:** resolved on 2026-05-12 (the recalibration bullet in `sdd/changes.md` was deleted; the user-observable AC changes have their own bullets, the threshold numerics live in AD36).
- **LOW — REQ-OPS-008 Notes field embedded a source file path:** resolved (the `Notes:` line has been removed; REQ-OPS-008 carries no `Notes:` field on the current spec).

## 2026-05-12 — spec-reviewer cycle-1 review bundle pass — RESOLVED

All actionable findings from this pass are now addressed:

- **2026-05-12 changelog block — entries vs. AC deltas:** resolved in the same pass.
- **MEDIUM — `src/queue/cleanup.ts` missing `// Implements REQ-PIPE-006` annotation:** resolved. The file's header now carries `// Implements REQ-PIPE-006 (orphan scrape-run retirement, AC 8)` alongside the existing REQ-PIPE-003/005/007 and REQ-DISC-006 annotations.
- **LOW — REQ-PIPE-001 AC 10 scrape_wait cap test missing:** resolved. `tests/queue/pipeline-consumer.test.ts` now contains `REQ-PIPE-001 AC 10 (CF-023): scrape_wait exceeding SCRAPE_WAIT_MAX_ITERATIONS marks scrape_run failed and pipeline failed` covering the iteration-budget exhaustion path.

## 2026-05-12 — informational notes that do not need spec changes

- **LOW (informational) — REQ-PIPE-001, REQ-PIPE-003, REQ-AUTH-001 sit in the 26-50-line LOW band.** Length is from genuine AC surface area, not implementation prose. Splitting is a JUDGMENT call (no natural boundary). Left as-is; revisit only if a future AC addition pushes one of them into the MEDIUM band.

No findings open. The next spec-reviewer / doc-updater pass starts from a clean slate.

## 2026-05-12 — doc-updater full --scope=all audit pass — RESOLVED

All findings applied.

**Findings resolved in this pass (applied in commits on `develop`):**
- Pass 6 HIGH (×4): `doc-allow-large` hatches in architecture.md (×2), deployment.md,
  decisions/README.md referenced non-existent ADR IDs `AD46a/b/c`. Fixed to `AD46`.
- Pass 6 MEDIUM (×2): Two `doc-allow-large` HTML comments embedded as backtick-code examples
  inside the AD46 prose body were flagged as bare markers. Replaced with plain text.
- Pass 5 MEDIUM (×52): api-reference.md (35 sections) and api-reference-admin.md (8 sections)
  missing **Method:**, **Path:**, and other per-item template fields. All added.
  Structured log events section marked `doc-template-exempt` (not an HTTP endpoint).
- Pass 5 MEDIUM (×9): ADRs AD1-AD7 and AD30 missing **Consequences:** and/or **Context:**.
  All added with real content derived from each ADR's existing rationale.
- Pass 1 MEDIUM (×5): Oversized table cells in architecture.md and configuration.md trimmed.
- Pass 1 MEDIUM (×8): Oversized list items in deployment.md, security.md, configuration.md trimmed.
- Pass 1 MEDIUM (×61): All 61 over-40-word ADR bullets in decisions/README.md trimmed. Zero
  bullets over 40 words remaining.

## 2026-05-12 — doc-updater PR #258 verification pass — RESOLVED

All escalated items have been addressed by a user-directed sweep:

- AD11 / AD40 / AD41 / AD42 oversized bullets (8 total) trimmed in place; load-bearing detail (cursor/window dead-zone, single-rerank silent-drop, etc.) preserved in the existing prose sections (`**Context:**`, `**Rationale:**`).
- AD39 context paragraph + AD40/AD41 rationale paragraphs split at natural sentence boundaries to fall under the 120-word cap.
- Additional housekeeping fixes from the same verification pass: `api-reference.md` `GET /` missing `**Response:**` field, `api-reference-admin.md` 18-line JSON response hatched (AD46 dedup-diag), `architecture.md` section 5.4 flow diagram hatched (AD46 diagram exemption), `deployment.md` two dev-bypass-runbook bash blocks hatched (AD46), `api-reference.md` 131-word admin-auth blockquote broken into three paragraphs, `architecture.md` 153-word scrape-finalize-consumer detail broken into three paragraphs, `configuration.md` 140-word dedup-threshold calibration note broken into three paragraphs.

Cross-file verification: zero bullets over 40 words and zero paragraphs over 120 words across the corpus.

No findings open. The next spec-reviewer / doc-updater pass starts from a clean slate.

## 2026-05-13 — spec-reviewer cycle-4 /sdd clean --scope=all pass — RESOLVED

All six MEDIUM escalations and the one HIGH from this pass were addressed in commits `c6cd951` (test-name migration: describe blocks across `tests/settings/discovery-status.test.ts`, `tests/reading/digest-page.test.ts`, `tests/settings/api.test.ts`, `tests/reading/error-pages.test.ts` renamed to dual-cite the split-child REQ IDs) and `e1b916c` (REQ-OPS-006 re-promoted Implemented after `Verification: Manual check` skip clause; `deploy-integration.yml` header carries `Implements REQ-OPS-006` backlink). The JUDGMENT informational on REQ-AUTH-008 storage-shape tokens is covered by the AD47-promoted corpus-wide allowlist carveout in `spec-discipline.md` line 126 — no per-REQ override needed.

Original escalations (preserved for audit):

**Applied this pass (no action needed):**

- **HIGH — REQ-OPS-006 (Integration deployment target) auto-demoted Implemented → Partial.** No test file literally cites the REQ ID, so `enforce_tdd: true`'s literal-match rule demotes. The `deploy-integration` workflow exercises the topology in production but does not count. `Notes:` field added naming the gap. Behavioral observation → changelog entry recorded.

**Escalated to owner (CQ-1 — not auto-fixable, test-author lane):**

- **MEDIUM — CQ-1 `req-test-name-only-match` — REQ-OPS-005 force-refresh tests.** `tests/pipeline/force-refresh.test.ts` covers AC 1+2+4 with real assertions (POST/GET 303 redirect, JSON 200 happy path, 120s reuse-window guard, three-layer admin gate with `CF_ACCESS_AUD` perimeter + session + email match). The REQ ID appears in the file header comment lines 1, 11, 12, 13 only — never inside a `describe` / `it` block name. CQ-1 requires the REQ ID in a block name AND assertions on AC content; the assertions are real, the block-name surfacing is missing. Recommended fix: rename one of the two existing top-level describes (`describe('POST /api/admin/force-refresh', …)` and `describe('GET /api/admin/force-refresh', …)`) to include `REQ-OPS-005`, identical in shape to the cycle-1 `REQ-HIST-001 / REQ-READ-001 AC 7:` rename PR.

**Escalated to owner (AC-vs-test count mismatch — test-authoring work):**

- **MEDIUM — REQ-DES-001 (Swiss-minimal visual language): 6 ACs vs 5 named tests.** `tests/e2e/design-tokens.spec.ts` has a `test.describe('REQ-DES-001 design tokens - computed values on /digest')` with 2 nested tests, plus `tests/reading/error-pages.test.ts` references it. One AC bullet lacks a dedicated test by name.
- **MEDIUM — REQ-DISC-002 (Discovery progress visibility): 3 ACs vs 1 named test.** `tests/settings/discovery-status.test.ts` covers all three ACs in practice, but four of the five `it()` blocks are named after `REQ-SET-006:` and only one carries `REQ-SET-006 / REQ-DISC-002:`. Rename two more `it()` blocks to dual-cite REQ-DISC-002.
- **MEDIUM — REQ-OPS-005: 5 ACs vs 0 named tests.** Same root cause as the CQ-1 finding above; the fix is one rename.
- **MEDIUM — REQ-READ-005 (Empty state copy): 3 ACs vs 1 named test.** `tests/reading/digest-page.test.ts:43` is the only test naming REQ-READ-005 (`empty state reads "No news for you today …"`). AC 2/3 covered in adjacent REQ-READ-001-named tests but not cited.
- **MEDIUM — REQ-SET-005 (Email notification preference): 4 ACs vs 1 named test.** `tests/settings/api.test.ts:461` carries the only REQ-SET-005-named `it()`. Other email-toggle behaviours covered in adjacent REQ-SET-002 / REQ-SET-003 named tests but not cited.

**Escalated to owner (JUDGMENT — informational, no action required this cycle):**

- **MEDIUM informational — REQ-AUTH-008 column-name references not yet covered by AD9.** AC 2/4/5 reference `parent_id`, `revoked_at`, `session_version`, `users.session_version`. **AD9** ("Storage-shape names are the persistence contract") already lists `mechanism-leakage:REQ-AUTH-002`, `REQ-DISC-001`, `REQ-DISC-002`, `REQ-SET-001`, `REQ-SET-005`, `forbidden-content-column-name:REQ-MAIL-001` — REQ-AUTH-008 is the sibling refresh-token REQ that fits the same pattern but is not enumerated. Two natural resolutions:
  1. Extend AD9's `Overrides:` line to include `mechanism-leakage:REQ-AUTH-008` — cleanest fit; preserves load-bearing detail AD9 says is the contract.
  2. Rewrite REQ-AUTH-008 AC bullets to omit column names — loses persistence-contract detail and creates asymmetry with REQ-AUTH-002.
  Conservative recommendation: option (1). Left for owner decision; no spec edit applied this pass.

**Not flagged this pass (false-positive filters held):**

- The two long ACs in REQ-AUTH-001 (AC 8 perimeter / baseline layers, AC 9 rate-limit policy) match the run-on detector by raw word count, but use the prescribed `a./b./c./d./e.` sub-bullet convention. False positive; not a run-on AC.
- REQ-OPS-008 (Unified admin pipeline run) initially flagged 7 ACs vs 0 named tests; refined describe-nesting count finds 14 nested `it()` blocks under two `describe(…REQ-OPS-008…)` parents across `tests/admin/pipeline-run.test.ts` and `tests/queue/pipeline-consumer.test.ts`. Fully covered.

No CRITICAL findings. Round counter ends at 1 (this pass commits to `sdd/` lane; previous `[sdd-clean]` no-op is excluded). The next non-agent commit to `sdd/` resets to 0.

## 2026-05-13 — `/sdd clean --all --unleashed` pass — RESOLVED

Both JUDGMENT escalations from this pass (REQ-PIPE-001 at 16 ACs, REQ-PIPE-003 at 17 ACs) were addressed by the binding AC-count cap sweep rounds 1 and 2 (sections below at lines 155 and 197). REQ-PIPE-001 split into REQ-PIPE-001 + REQ-PIPE-010 (body-fetch) + REQ-PIPE-011 (candidate filtering); REQ-PIPE-003 split into REQ-PIPE-003 + REQ-PIPE-012 (matching policy) + REQ-PIPE-013 (cross-tick automation) + REQ-PIPE-014 (operator surfaces). Final corpus state: zero Active REQs over the 7-AC cap.

Original entry (preserved for audit):

- **SAFE — REQ-PIPE-001 AC 8 sub-bullet promotion (`sub-bullets-banned`).** AC 8's `a./b./c./d./e.` body-fetch sub-bullets promoted to top-level ACs 8–13 verbatim. Existing AC 9–11 shifted to AC 14–16. No clause dropped; mechanical re-render of nesting.
- **SAFE — `documentation/architecture.md` file-level `doc-allow-large` marker added.** File was 442L over the 350L Pass 2 budget; marker `<!-- doc-allow-large: AD46 architecture-doc diagram-section exemption -->` placed in the preamble cites the existing AD46b carveout that already covers this file's diagram-pluss-prose contiguity requirement. The three element-level markers at the diagrams stay as-is (they exempt the diagrams themselves from Pass 1 caps, which is a separate rule).

**JUDGMENT escalations (no auto-fix; owner decision):**

- **MEDIUM — REQ-PIPE-001 now has 16 ACs (>10) after sub-bullet promotion.** Single Admin actor; the AC list mixes coordinator-level concerns (cadence, dedup, source-list construction, blocklist) with body-fetch detail (the promoted ACs 8–13). Two plausible splits:
  1. Extract ACs 8–13 (body-fetch behaviour) to a new REQ-PIPE-009 in `sdd/generation.md`, leaving AC 8 as a single parent bullet "body-fetch behaviour governed by REQ-PIPE-009." Mechanical along a concern boundary but the concern is genuinely sub-feature, not cross-cutting policy — so the "cross-cutting concerns" auto-extract rule does not apply. This is a sub-feature carve-out and stays JUDGMENT.
  2. Merge the six body-fetch ACs back into one prose AC (run-on risk; >150 words). Would re-trigger the run-on rule.
  Conservative recommendation: option (1). Defer to owner.
- **MEDIUM — REQ-PIPE-003 has 17 ACs (>10).** Single Admin actor; all ACs describe the same job (same-story dedup). No clean actor axis (single actor) and no clean cross-cutting-concern axis (every AC is dedup-specific). A semantic split (URL-canonicalisation vs cross-tick matching vs operator surfaces vs retention coupling) is plausible but each candidate boundary requires Intent re-interpretation, which is the JUDGMENT case the rule reserves for owner decision. No auto-fix.

**LOW informational (no auto-fix per rule):**

- `sdd/email.md` — REQ-MAIL-001 has 10 ACs and 27 lines (single actor + single concern → LOW smell, no auto-fix).
- `sdd/generation.md` — REQ-PIPE-001 has 10 ACs at 32 lines (now 16 ACs after promotion above); REQ-PIPE-003 has 17 ACs at 34 lines.
- `sdd/reading.md` — REQ-READ-007 has 8 ACs.

Length / AC-count smells in single-actor, single-concern REQs are informational only. Mergeable ACs are an owner judgment; the rule does not auto-merge prose.

No CRITICAL findings. No HIGH findings. Round counter ends at 1 (this `[sdd-clean]` commit is excluded). Next non-agent commit to `sdd/` resets to 0.

---

## 2026-05-13 — binding AC-count cap follow-up

After the cycle-4 spec-discipline update made the ≤7 AC cap binding (8-10 ACs single-actor + single-concern now MEDIUM mandatory-split; >10 ACs HIGH mandatory-split), the prior "LOW informational" cluster was re-triaged:

- **Resolved.** REQ-PIPE-001 (originally 10 ACs) split into REQ-PIPE-001 + REQ-PIPE-010 (body-fetch) + REQ-PIPE-011 (candidate filtering) earlier in this run.
- **Resolved.** REQ-PIPE-003 (17 ACs) split into REQ-PIPE-003 + REQ-PIPE-012 (matching policy variants) + REQ-PIPE-013 (cross-tick automation + retention) + REQ-PIPE-014 (operator dedup surfaces) earlier in this run.
- **Resolved.** REQ-MAIL-001 (10 ACs) split into REQ-MAIL-001 (content) + REQ-MAIL-003 (send policy). AD9 Overrides extended to cover the `email_enabled` column reference now carried by REQ-MAIL-003 AC 3.
- **Resolved.** REQ-READ-007 (9 ACs) split into REQ-READ-007 (pop / hold / cascade motion) + REQ-READ-008 (scroll, wrap, fallback). AD19 backlinks updated to reference both REQs and to repair a pre-existing stale anchor (`req-read-007-tag-railing-cascade-on-toggle-and-add` → current heading slug).

Documentation backlinks under `documentation/configuration.md` and `documentation/architecture.md` re-anchored to the new MAIL-001 heading and partitioned across MAIL-001 / MAIL-003 by module concern (content rendering vs. dispatcher).

No CRITICAL findings remain. No HIGH findings remain. No MEDIUM findings remain in the cap-bound category.

---

## 2026-05-13 (later pass) — `/sdd clean --all` JUDGMENT informational — RESOLVED (false-positive)

The JUDGMENT informational escalation is a false-positive. `~/.claude/rules/spec-discipline.md` line 126 already carries the corpus-wide carveout the escalation asked for: *"Database column / KV key names when the storage shape IS the persistence contract... promoting it to a first-class allowlist retires the per-REQ ADR-override mechanism."* AD47 (lines 1387-1410 of `documentation/decisions/README.md`) documents this carveout's promotion from AD9. The 5 storage-shape REQs (REQ-AUTH-008, REQ-DISC-001, REQ-MAIL-003, REQ-PIPE-001, REQ-SET-001) pass the rule by default; no per-REQ `Overrides:` header is needed because the carveout is in the discipline rule itself, not in any individual ADR.

Spec-reviewer over-flagged because the rule's documentation carveout was not cross-linked to AD9's prior per-REQ enumeration list. No spec edit warranted.

Original escalation (preserved for audit):

- **JUDGMENT informational — storage-shape token references across 11 Implemented REQs are not enumerated in any ADR `Overrides:` header after AD9 supersession.** Affected REQs (token in parens):
  - `sdd/authentication.md` — REQ-AUTH-001 (`no_verified_email` — error code, user-facing), REQ-AUTH-003 (`forbidden_origin` — error code, user-facing), REQ-AUTH-004 (`access_denied` — error code, user-facing), REQ-AUTH-008 (`revoked_at` — column name, persistence contract)
  - `sdd/discovery.md` — REQ-DISC-001 (`pending_discoveries`, `added_at` — table+column, persistence contract)
  - `sdd/email.md` — REQ-MAIL-003 (`email_enabled` — column, persistence contract)
  - `sdd/generation.md` — REQ-PIPE-001 (`scrape_runs` — table name, persistence contract)
  - `sdd/observability.md` — REQ-OPS-002 (multiple error codes: `llm_invalid_json`, `llm_failed`, `all_sources_failed`, `generation_stalled`, `user_cancelled`, `error_code`, `access_denied`, `no_verified_email`, `invalid_state`, `oauth_error` — all user-facing strings, already admitted by `user_facing_strings: true`)
  - `sdd/pwa.md` — REQ-PWA-001 (`short_name`, `theme_color`, `background_color` — web manifest spec field names, protocol contract, admitted by `protocols: true`)
  - `sdd/reading.md` — REQ-READ-006 (`error_code` — user-facing wire shape)
  - `sdd/settings.md` — REQ-SET-001 (`hashtags_json`, `digest_hour` — column names, persistence contract)

The "user-facing strings" and "protocols" subset (REQ-AUTH-001/003/004, REQ-OPS-002, REQ-PWA-001, REQ-READ-006) is already covered by the active `forbidden_content_allowlist` keys in `sdd/config.yml`. The remaining 6 items (REQ-AUTH-008, REQ-DISC-001, REQ-MAIL-003, REQ-PIPE-001, REQ-SET-001) carry true storage-shape names and were historically covered by AD9's `Overrides:` line, which no longer exists after AD9's supersession.

Two natural resolutions (preserved from the cycle-4 entry, broadened to all 6 REQs):

1. **Reinstate the override surface via a successor ADR.** Add a new ADR documenting "Storage-shape names in REQ ACs are the persistence contract" with an `**Overrides:**` header listing `mechanism-leakage:REQ-AUTH-008`, `mechanism-leakage:REQ-DISC-001`, `mechanism-leakage:REQ-MAIL-003`, `mechanism-leakage:REQ-PIPE-001`, `mechanism-leakage:REQ-SET-001`. Preserves load-bearing detail and matches the post-AD9 override surface that spec-reviewer reads.
2. **Add a `storage_shape_names: true` key to `forbidden_content_allowlist` in `sdd/config.yml`.** Broader (corpus-wide allowance, not per-REQ), but matches the configuration framework already in place for `vendors`, `protocols`, `user_facing_strings`, etc.

Conservative recommendation: option (1). The per-REQ enumeration is more honest about which REQs carry persistence-contract names; future REQ additions that drift toward forbidden content are caught instead of silently admitted under a corpus-wide bypass.

No spec edit applied this pass. Defer to owner.

---

## 2026-05-13 — binding AC-count cap sweep (round 2)

After the MAIL-001 / READ-007 splits, a corpus-wide sweep that honours `Status: Deprecated` (the prior dry-run script did not) surfaced three additional active REQs over the binding cap. The user chose "Split all three now" so all are applied in the same session.

- **Resolved.** REQ-PIPE-002 (8 ACs) split by sub-feature into REQ-PIPE-002 (chunked LLM output content contract, 4 ACs) and REQ-PIPE-015 (chunk processing robustness, 3 ACs).
- **Resolved.** REQ-PIPE-006 (8 ACs) split by sub-feature into REQ-PIPE-006 (recording + read surfaces + live-progress status endpoint, 5 ACs) and REQ-PIPE-016 (idempotency on redelivery + stuck-run cleanup, 3 ACs).
- **Exempt.** REQ-PIPE-008 (11 ACs) is `Status: Deprecated` and per the spec-discipline carveout is not subject to structural rules. The earlier dry-run that flagged it did not honour the carveout; the corrected sweep treats it as exempt.
- **Resolved.** REQ-SET-002 (8 ACs) split by sub-feature into REQ-SET-002 (hashtag curation strip UX, 4 ACs) and REQ-SET-008 (hashtag persistence, validation, and defaults, 4 ACs).
- **Resolved.** REQ-HIST-001 (8 ACs) split by sub-feature into REQ-HIST-001 (day-grouped article history, 5 ACs) and REQ-HIST-003 (search, tag filter, and deep-link on /history, 3 ACs).

Documentation backlinks under `documentation/architecture.md`, `documentation/api-reference.md`, `documentation/configuration.md`, and `documentation/decisions/README.md` re-anchored where headings changed (PIPE-002 + SET-002) and re-partitioned by sub-concern where the cited AC moved (PIPE-006 AC 8 → PIPE-016 AC 3, SET-002 AC 8 → SET-008 AC 4, HIST-001 AC 4/5 → HIST-003 AC 1/2). `src/pages/api/tags/restore.ts` and `delete-initial.ts` REQ-ID comments re-pointed to REQ-SET-008. `pending.md`'s SET-002 AC 8 entry re-pointed to SET-008 AC 4.

---

## 2026-05-13 — `/sdd clean --all --unleashed` Phase 3 (new framework rules applied)

This pass applied the AC granularity rule ("if an AC can be split into >=2 distinct test names, it MUST be split"), the cross-reference anchor-link rule, the blank-line policy on trailing fields, the Deprecated-REQ deletion rule (no tombstone), and the chain enforcement that promotes granulate -> cap check -> sub-feature split.

**Spec-side findings applied this pass (no action needed):**

- **HIGH (Deprecated deletion) -- 10 REQs deleted.** Per the new "Deprecated REQs are deleted" rule (no tombstones), the following `Status: Deprecated` entries were removed in Phase 2: REQ-READ-004 (-> REQ-PIPE-001), REQ-PIPE-008 (-> REQ-PIPE-003), REQ-GEN-001/002/003/007 (-> REQ-PIPE-001), REQ-GEN-004 (-> REQ-PIPE-003), REQ-GEN-005/006 (-> REQ-PIPE-002), REQ-GEN-008 (-> REQ-HIST-002). Tokenization check confirmed no load-bearing clauses lost; successor REQs already carry the live contract. Changelog entry recorded.
- **MEDIUM (AC granularity) -- 18 ACs split + 1 rewrite.** Per the new granularity signal "if an AC can be split into >=2 distinct test names, it MUST be split", the following multi-behaviour ACs were granulated in place (each AC moved verbatim or with whitespace-only edits): REQ-SET-001 AC 5, REQ-READ-008 AC 1, REQ-READ-002 AC 4, REQ-PIPE-014 AC 1, REQ-OPS-008 AC 1, REQ-PIPE-001 AC 4, REQ-DES-002 AC 6, REQ-SET-008 AC 4, REQ-PWA-003 AC 4, REQ-PIPE-009 AC 5, REQ-DES-002 AC 1, REQ-HIST-003 AC 2, REQ-PIPE-003 AC 3, REQ-DISC-004 AC 4, REQ-PIPE-002 AC 3, REQ-PWA-001 AC 1, REQ-READ-002 AC 1, REQ-PIPE-006 AC 5. REQ-PIPE-003 AC 6 sentence 1 dropped as redundant with the new AC 3/4 (tie-break + earlier-publication-time survives now live in REQ-PIPE-018).
- **MEDIUM (chain enforcement) -- 6 REQ-level splits.** Per the chain rule (granulate -> if >7 ACs, run Splitting by sub-feature), six REQs that exceeded the cap after AC granulation were split into sibling REQs along the concern-boundary axis. Each AC moved verbatim; Intent rewritten per sub-feature. Parent-child link via Dependencies (not Notes).
  - REQ-OPS-008 (Unified admin pipeline run from the settings surface, 8 ACs) -> REQ-OPS-008 (run trigger, 4 ACs) + REQ-OPS-009 (run progress surface, 4 ACs).
  - REQ-PIPE-001 (Global scrape-and-summarise pipeline on a fixed cadence, 9 ACs after granulation) -> REQ-PIPE-001 (orchestration + coverage, 7 ACs) + REQ-PIPE-017 (article-pool ingestion contract: URL dedup, source-list aggregation, first-ingestion timestamp preservation, per-item publisher resolution, 4 ACs).
  - REQ-PIPE-003 (Same-story dedupe core matching contract, 9 ACs after granulation) -> REQ-PIPE-003 (detection + timing rules, 4 ACs) + REQ-PIPE-018 (collapse mechanics: survivor selection, tie-break, aggregator-vs-direct, data merge, single-source persistence, 5 ACs).
  - REQ-DES-002 (Light and dark mode with no flash, 8 ACs after granulation) -> REQ-DES-002 (light/dark toggle + token system, 6 ACs) + REQ-DES-004 (mobile and PWA no-flash chrome: status bar + body never flashes opposite theme, 3 ACs).
  - REQ-PWA-003 (Mobile-first responsive layout, 10 ACs after granulation) -> REQ-PWA-003 (layout + tap-targets, 5 ACs) + REQ-PWA-004 (brand link interaction contract: home/scroll-to-top/clear-filter/modifier-clicks/tap-target geometry, 5 ACs).
  - REQ-READ-002 (Article detail view, 9 ACs after granulation) -> REQ-READ-002 (article detail view rendering, 5 ACs) + REQ-READ-009 (article detail return navigation and source affordance, 5 ACs).
- **MEDIUM (cross-reference anchor linking) -- 131 cross-ref lines linked.** Per the new "Cross-reference linking (binding)" rule, every `REQ-*` and `CON-*` token inside `**Constraints:**` and `**Dependencies:**` field values was rewritten as a GitHub-flavoured-Markdown anchor link (88 anchors built across 11 sdd/*.md files).
- **MEDIUM (blank-line policy) -- 334 blank lines inserted.** Per the new "Blank-line policy (binding)" clause on trailing fields, one blank line was inserted between every labelled field in the trailing block (`Constraints` / `Priority` / `Dependencies` / `Verification` / `Status`) so they no longer collapse into one rendered paragraph on GitHub.

**Escalated to owner (CQ-1 -- test-name migration for new REQ IDs):**

- **MEDIUM -- CQ-1 `req-test-name-only-match` -- six new REQs from this pass (REQ-OPS-009, REQ-PIPE-017, REQ-PIPE-018, REQ-DES-004, REQ-PWA-004, REQ-READ-009) carry behaviour that is already test-covered under the parent REQ ID, but no test file yet cites the new IDs.** The carve-outs are pure spec restructuring (no shipped-behaviour change); each child's ACs were lifted verbatim from the parent. Recommended fix: rename the most-relevant existing describe in each test file by appending ` / REQ-X-NNN (one-line concern)` to dual-cite the parent and the new child REQ. Identical shape to the cycle-1 `REQ-HIST-001 / REQ-READ-001 AC 7:` rename and the cycle-4 `REQ-SET-002 / REQ-SET-008` rename. Touched test files (initial mapping; owner verifies):
  - REQ-OPS-009 -> `tests/admin/pipeline-run.test.ts`, `tests/queue/pipeline-consumer.test.ts` (existing REQ-OPS-008 nested describes; rename the four progress-surface `it()` blocks)
  - REQ-PIPE-017 -> `tests/pipeline/coordinator.test.ts`, `tests/pipeline/finalize-vectorize.test.ts` (the URL-dedup, source-list-aggregation, first-ingestion-timestamp, and per-item-publisher tests already in REQ-PIPE-001 family)
  - REQ-PIPE-018 -> `tests/pipeline/finalize-vectorize.test.ts`, `tests/pipeline/bidirectional-dedup.test.ts`, `tests/pipeline/historical-dedup.test.ts` (the survivor-selection, tie-break, aggregator-vs-direct, data-merge tests already in REQ-PIPE-003 family)
  - REQ-DES-004 -> `tests/e2e/theme-toggle.spec.ts` and the existing REQ-DES-002 describes (the mobile status-bar and no-flash-between-routes cases)
  - REQ-PWA-004 -> the existing REQ-PWA-003 brand-link tests across `tests/reading/page.test.ts` and the e2e suite (back-to-top, clears-filter, modifier-click-passthrough, tap-target-geometry)
  - REQ-READ-009 -> `tests/reading/page.test.ts`, `tests/reading/digest-page.test.ts`, the article-detail e2e suite (back-navigation, View Transitions reverse morph, source modal, modal-close)

  Auto-fix in `auto`/`unleashed` is the test-name rename in each owner-named file. spec-reviewer left Status: Implemented on each new REQ (behaviour ships and is tested under the parent ID); the rename is a discoverability fix, not a coverage-presence fix.

**Spec-only manifest passes (this run):**

- Forbidden content in REQs: clean across 11 files.
- Status field semantics + Deprecated cleanup: 10 Deprecated REQs deleted (above).
- REQ rendering template (binding): cross-ref linking + blank-line policy applied corpus-wide.
- REQ length guidance: post-split, no Active REQ exceeds 100 lines; REQ-OPS-008 sits at 8 ACs (MEDIUM, deferred to a future concern-boundary pass).
- Acceptance criteria guidance + AC granularity + REQ accretion guard: 18 splits applied (above).
- Actor coherence: clean (every AC in every Active REQ matches its `Applies To:`).
- Sub-bullets in ACs banned: clean (no indented list items under numbered ACs).
- Cross-cutting concerns: no new policy-shape ACs detected in feature REQs.
- Concern-boundary split: 6 REQ-level splits applied (above).
- Mechanism leakage in AC bullets: clean against current `forbidden_content_allowlist`.
- Changelog drift: this pass adds no changelog entries (pure spec restructuring; behaviour unchanged).
- Meta-content leakage Rule A (stub-after-extraction): clean across 11 files (no ≤1-AC pointer-only REQs).
- Meta-content leakage Rule B (Notes two-shape): clean (every Active Notes field carries a markdown link to documentation/** or sdd/**, or is the Partial-explanation shape ≤3 sentences).
- Meta-content leakage Rule C (preamble edit-history): clean across 11 domain files (no ISO dates, edit verbs, or rule-name patterns in any preamble).
- CQ-1 -- REQ-test truth-check: 6 new REQ IDs escalated as Subclass B (above).
- CQ-2 -- vendor/external-interface drift: deferred to a follow-up pass.
- CQ-3 -- content-preservation on shrink: inert (no shrink ops this run; only re-rendering and split, which preserve content by construction).
- Backlog re-triage: prior open findings all resolved; this entry replaces them with the Phase 3 audit.

Corpus-wide dry-run after the second sweep: zero active (non-Deprecated) REQs over the 7-AC cap. The binding-cap principle now holds uniformly.
