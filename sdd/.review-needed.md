# Review Needed

Items requiring human attention. Clear entries as they are resolved.

## 2026-04-27 — HIGH: REQ-PIPE-008 AC 9 rollback-failure sub-case has no test

**Trigger:** commit `e5b8788` (`fix(pipe): guard finalize-lock rollback so a D1 outage doesn't strand the lock`) wraps the lock-clearing UPDATE in its own try/catch and logs `finalize_lock_rollback_failed` when the rollback itself fails. AC 9 of REQ-PIPE-008 was refined in the same push to honestly describe this sub-case.

**Finding:** `tests/pipeline/chunk-consumer.test.ts` covers the happy-path rollback (`REQ-PIPE-008: send-failure rollback — clears finalize_enqueued so the queue retry can re-attempt`) but no test exercises the rollback-failure path: the case where `SCRAPE_FINALIZE.send()` rejects AND the subsequent `UPDATE scrape_runs SET finalize_enqueued = 0` ALSO throws. The new `finalize_lock_rollback_failed` log emission and the "original sendErr is the one thrown" guarantee in AC 9 are therefore unverified by automated tests.

**Why escalated (not auto-demoted):** The REQ as a whole is well-tested (40+ test references); demoting it to `Partial` because of one uncovered sub-case would be too coarse and obscure the broader truth. spec-discipline rule 5b applies strictly to source-with-no-test, but here the source has tests — just not for this specific defensive branch. JUDGMENT call: write a test, accept the gap as defensive-only-coverage, or capture as an explicit `pending.md` item.

**Proposed resolution (please pick one):**
1. Add a test in `tests/pipeline/chunk-consumer.test.ts` that mocks both `SCRAPE_FINALIZE.send()` rejection AND a `UPDATE scrape_runs` rejection on the rollback statement, asserts that the original `sendErr` is the one re-thrown, and asserts that `log('error', 'digest.generation', { status: 'finalize_lock_rollback_failed', ... })` was emitted with `send_error` and `rollback_error` fields.
2. Record the gap in `pending.md` with a rationale ("defensive double-fault path; manual code review accepted").
3. Add a `.user-overrides.md` entry keyed `test-coverage:REQ-PIPE-008-AC9-rollback-failure` to silence future spec-reviewer findings on this specific sub-case.

## 2026-04-25 — HIGH: Admin force-refresh endpoint has no REQ

**Trigger:** commit `b65339e` changed the GET response shape of `/api/admin/force-refresh` (browsers now get a 303 to `/settings`, scripts opt into JSON via `Accept: application/json`).

**Finding:** No REQ in `sdd/` documents the operator force-refresh contract. The source file `src/pages/api/admin/force-refresh.ts` carries `Implements REQ-OPS-004`, but REQ-OPS-004 is about crawler policy — the annotation is incorrect. The Admin actor is named in `sdd/README.md` as having "force a fresh scrape tick" capability, but no REQ defines the endpoint's behaviour, transports, or auth gate.

**Why escalated (not auto-fixed):** Creating a new REQ across domains is a JUDGMENT call. Conservative unleashed rule: never invent a new REQ when domain placement is ambiguous (observability vs generation).

**Proposed resolution (please pick one):**
1. Add `REQ-OPS-005: Admin force-refresh endpoint` in `sdd/observability.md` covering both transports (303 default, JSON on Accept), Cloudflare Access gating, and the REUSE_WINDOW_SECONDS guard. Update annotation in `force-refresh.ts` from `REQ-OPS-004` to `REQ-OPS-005`.
2. Fold the contract into a widened `REQ-GEN-002: Manual refresh` and split user-facing vs operator-triggered paths.
3. Treat as out-of-scope operator tooling and remove the misleading `REQ-OPS-004` annotation from the source file.

