# Review Needed

Items requiring human attention. Clear entries as they are resolved.

## 2026-04-25 — HIGH: Admin force-refresh endpoint has no REQ

**Trigger:** commit `b65339e` changed the GET response shape of `/api/admin/force-refresh` (browsers now get a 303 to `/settings`, scripts opt into JSON via `Accept: application/json`).

**Finding:** No REQ in `sdd/` documents the operator force-refresh contract. The source file `src/pages/api/admin/force-refresh.ts` now carries three annotations — `Implements REQ-PIPE-001`, `Implements REQ-OPS-004`, and `Implements REQ-AUTH-001` (the REQ-AUTH-001 annotation was added in the rate-limit hardening branch and is correct because the file enforces the three-layer admin gate). REQ-PIPE-001 and REQ-AUTH-001 are accurate cross-cuts, but the file's primary subject — the operator-triggered out-of-band scrape tick with REUSE_WINDOW_SECONDS guard and content-negotiated 303-vs-JSON transport — is not described by any REQ. The lingering `REQ-OPS-004` annotation is also still incorrect (REQ-OPS-004 is about crawler policy, not operator tooling). The Admin actor is named in `sdd/README.md` as having "force a fresh scrape tick" capability, but no REQ defines the endpoint's behaviour, transports, or auth gate.

**Latest state (2026-04-27):** `head -3 src/pages/api/admin/force-refresh.ts` shows the three current annotations. No code change to the endpoint's contract this push — the gap is structural and unchanged.

**Why escalated (not auto-fixed):** Creating a new REQ across domains is a JUDGMENT call. Conservative unleashed rule: never invent a new REQ when domain placement is ambiguous (observability vs generation).

**Proposed resolution (please pick one):**
1. Add `REQ-OPS-005: Admin force-refresh endpoint` in `sdd/observability.md` covering both transports (303 default, JSON on Accept), Cloudflare Access gating, and the REUSE_WINDOW_SECONDS guard. Update annotation in `force-refresh.ts` from `REQ-OPS-004` to `REQ-OPS-005`; keep `REQ-PIPE-001` and `REQ-AUTH-001`.
2. Fold the contract into a widened `REQ-GEN-002: Manual refresh` and split user-facing vs operator-triggered paths.
3. Treat as out-of-scope operator tooling and remove the misleading `REQ-OPS-004` annotation from the source file (keep `REQ-PIPE-001` and `REQ-AUTH-001`, drop `REQ-OPS-004`).

## 2026-04-27 — RESOLVED: Stale "7 days" prose + REQ-HIST-001 AC 8 boundary test gap

**Original trigger:** PR6 (commit `117a527`) extended REQ-PIPE-005 retention and REQ-HIST-001 history window 7 → 14 days but left stale prose in source comments, test names, test assertions, and `documentation/`, plus no boundary test pinning the 14-day cutoff.

**Resolved by commit `f0f3a42` (PR6 follow-ups):**
- `src/queue/cleanup.ts:8` — comment bumped 7 → 14.
- `src/pages/history.astro:93, 150, 852` — comments bumped 7 → 14.
- `tests/pipeline/cleanup.test.ts:111` — renamed; fixture timestamps bumped from `daysAgo(8)/(10)` to `daysAgo(15)` so they actually exceed the 14-day window.
- **New boundary test at `tests/pipeline/cleanup.test.ts:143`** — `daysAgo(13)` survives, `daysAgo(15)` is deleted; explicitly cites REQ-HIST-001 AC 8 in the inline comment. This closes the AC 8 coverage gap: a future drift between `RETENTION_SECONDS` and `WINDOW_SECONDS` will surface in CI.
- `tests/history/api.test.ts` — module docstring + day-cap test bumped 7 → 14; seed bumped 10 → 16.
- `tests/history/page.test.ts:133` — empty-state assertion fixed from "last 7 days" to "last 14 days".
- `documentation/architecture.md` and `documentation/configuration.md` retention prose bumped 7 → 14 (with the stuck-tag `STUCK_TAG_TTL_SECONDS` 7-day window correctly preserved).

**Residual prose drift in `documentation/` for doc-updater to handle in this same push:**
- `documentation/architecture.md:96` — `GET /api/history` row still says "last 7 days".
- `documentation/architecture.md:284` — narrative still says "exempt from the 7-day cleanup".

These two are doc-updater's lane (not spec-reviewer's). They do not affect spec correctness.
