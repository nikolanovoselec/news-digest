# Review Needed

Items requiring human attention. Clear entries as they are resolved.

## 2026-04-24 — spec-reviewer pass on f5c6806..f15234e (deprecation + out-of-scope + polish)

Unleashed-mode spec-reviewer verified the merged work on `develop`: f5c6806 (the deprecation/out-of-scope merge commit) followed by f15234e (an immediate polish commit that split the changelog bullet and added `(was REQ-X-NNN)` parentheticals to the new Out-of-Scope lines). Round counter 0/3 (no agent-tagged commits in the last 3), branch-safety OK (develop, not main), working tree was clean at start. Findings below split into (a) already applied upstream, (b) applied by this pass, and (c) escalated because they live in another agent's lane or need a human judgement.

**Already applied upstream (f15234e):**
- **Changelog sentence count (LOW):** the initial 2026-04-24 bullet was 3 sentences. f15234e split it into two bullets (one for the Deprecated trio, one for the Out-of-Scope trio) so each stays ≤2 sentences.
- **Out-of-Scope REQ-ID preservation (HIGH):** the three new `sdd/README.md` Out-of-Scope bullets originally did not cite the removed REQ IDs. f15234e added `(was REQ-X-NNN)` parentheticals so the IDs remain greppable and the decision history survives. The full REQ bodies were not preserved — existing Out-of-Scope bullets in the same file follow a one-liner convention dating back to the MVP spec; bringing the 3 new bullets in line with that convention matches the existing 7 bullets. Flagging once here as a convention-level question for a future spec-wide cleanup rather than force-rewriting.

**Applied in-lane (sdd/) by this pass:**
- **User override recorded (JUDGMENT → APPLIED):** created `sdd/.user-overrides.md` with `deprecated-with-live-code:REQ-{DISC-002,DISC-003,SET-004}` entries. REQ-DISC-003 and REQ-SET-004 each have live source code + passing tests; strict spec-discipline semantics ("Deprecated = was built, then removed or replaced") would classify them as Partial/Implemented rather than Deprecated because the code and tests are still shipping. The user's explicit instruction on 2026-04-24 was Deprecated. Override recorded so subsequent spec-reviewer runs do not silently revert the Status.

**Escalated (out-of-lane or needs human):**
- **Broken markdown anchors in `documentation/` → doc-updater lane (HIGH):** `documentation/architecture.md:104` and `documentation/architecture.md:133` link to `../sdd/pwa.md#req-pwa-002-offline-reading-of-the-last-digest`, but REQ-PWA-002 was removed from `pwa.md` in this commit. The anchor no longer resolves. Doc-updater should retarget these to the new `sdd/README.md` Out of Scope anchor or drop the links entirely on its next run.
- **Deprecated-REQ anchors in `documentation/` (MEDIUM):** `documentation/architecture.md:82,120` and `documentation/api-reference.md:110,189` and `documentation/configuration.md:54` link to REQ-DISC-002, REQ-DISC-003, REQ-SET-004. These anchors still resolve (the REQs exist as Deprecated), but doc-updater may want to mark the rows as "Deprecated" so readers don't build new features against them.
- **Orphan `Implements REQ-*` annotations → code-reviewer lane (MEDIUM):** `src/lib/models.ts:1`, `src/pages/api/settings.ts:1`, `src/pages/settings.astro:2`, `src/components/ModelSelect.astro:2`, `src/lib/discovery.ts:1`, and `wrangler.toml:54` annotate themselves against REQ-SET-004 or REQ-DISC-003 — now Deprecated. Per spec-discipline, annotations for Deprecated REQs should either be removed (if dead code) or the REQs should not have been Deprecated (if still-live code). Because the user explicitly chose Deprecated (see `.user-overrides.md`), code-reviewer should decide on a case-by-case basis whether each annotation points at dead code (remove it) or intentionally-preserved code (leave the annotation and accept the spec-vs-code divergence).
- **Live tests for Deprecated REQs → tdd-guide / code-reviewer lane (MEDIUM):** `tests/lib/models.test.ts` has 13 assertions against REQ-SET-004; `tests/discovery/process-pending.test.ts` has 3 assertions against REQ-DISC-003; `tests/settings/api.test.ts` has 2 assertions against REQ-SET-004; `tests/generate/cron.test.ts` has 1 assertion against REQ-DISC-003. A test referencing a Deprecated REQ is not a spec-reviewer fix (tests are outside `sdd/`). Flag for human decision: keep the tests (they still verify the hidden-but-live code paths) or delete them alongside the eventual code cleanup.
- **Stale `pending.md` entries → out-of-lane (MEDIUM):** `pending.md:9` (REQ-DISC-002 AC 2/3) and `pending.md:18` (REQ-DISC-003 AC 1–5) still list follow-up work for the two now-Deprecated REQs. Deprecated REQs do not get finished. The user should delete these entries or move them into an "abandoned" section of pending.md. (Spec-reviewer does not touch files outside `sdd/`.)
- **Stale domain-table descriptions (LOW):** `sdd/README.md:30` still mentions "model selection" in the Onboarding & Settings row, and `sdd/README.md:31` mentions "health tracking" in the Source Discovery row. Both are now Deprecated. Minor; the domain table is a high-level blurb, not a REQ spec. Not auto-fixing because the wording change is a judgment call.

## 2026-04-24 — spec-reviewer pass on 0c8aa6a (PR #12, develop)

Unleashed-mode spec-reviewer ran against the full diff f2ea50e..0c8aa6a on `develop`. Working tree clean, round counter 0/3, branch-safety OK. Auto-applied edits are captured in the 2026-04-24 changelog; findings requiring human follow-up are below.

- **REQ-READ-001 AC 5 reconciled (HIGH → APPLIED):** the dashboard SQL (`src/pages/api/digest/today.ts`) orders by `ingested_at DESC, published_at DESC` with a LIMIT of 29. The prior AC text ("ordered by published-at descending") contradicted shipped behaviour. AC 5 updated to match. Tests at `tests/articles/feed.test.ts:237` still pass because the test fixture ingests all rows at the same `ingested_at`, so `published_at` becomes the tiebreaker and the assertion survives — but the test does NOT verify the new primary sort key. Follow-up for `tdd-guide`: add a case where two articles share published_at but differ in ingested_at, and assert the newer-ingested one wins.
- **REQ-PIPE-001 AC 7 added, auto-demoted to Partial (HIGH → APPLIED):** 48-hour freshness filter in `src/queue/scrape-coordinator.ts` has no automated test. Follow-up for `tdd-guide`: feed the coordinator a candidate with `published_at = nowSec - 49*3600` and assert it is dropped (and a `coordinator_drop_stale` log is emitted); feed one with `published_at = nowSec` and assert it is kept.
- **REQ-SET-007 reshaped, auto-demoted to Partial (HIGH → APPLIED):** the code in `src/layouts/Base.astro` POSTs silently on every page load when the browser tz differs from the stored tz. The prior AC described a confirmation banner; no such banner exists in the current UI. AC rewritten to match shipped silent behaviour. Follow-up for `tdd-guide`: assert Base.astro emits the inline module, assert it guards on `documentElement.dataset.tzAutoBound`, and integration-test the `/api/auth/set-tz` round-trip.
- **REQ-HIST-001 AC numbering fixed (MEDIUM → APPLIED):** the bullets were 1, 2, 3, 4, 7, 5, 6 after the PR #12 tag-railing insertion. Renumbered in place to 1–7. Content unchanged; no cross-references to AC numbers exist elsewhere so the renumber is safe.
- **Annotations (PASS):** new source file `src/components/TagStrip.astro` carries both `Implements REQ-READ-001` and `Implements REQ-HIST-001`. `src/queue/scrape-coordinator.ts`, `src/pages/api/digest/today.ts`, `src/layouts/Base.astro` all annotated. No missing annotations.
- **Changelog shape (PASS):** 2026-04-24 bullets are dated, each under 2 sentences, user-facing, no commit SHAs, no verification prose.
- **Forbidden content (PASS):** no new hex codes, file paths, function names, HTTP status codes, JSON schemas, or regex literals in the new AC text. The prior "gradient-vertical icon" leakage was already resolved in-commit to "centred list-style icon". No new LOW findings.

Cleared from prior entry: the branch-safety block at the top of this file dated 2026-04-24 applied to a `main` run; this pass is on `develop` and did auto-commit. That older block can be removed once its findings are confirmed landed.

## 2026-04-24 — branch-safety block: spec-reviewer cannot auto-commit on `main`

Unleashed-mode spec-reviewer ran against the 6-commit batch since `7c09e34` (f0a51bc..25fe042) but refused to auto-commit because the working branch is `main` and no `--branch-confirmed` flag was supplied (per `spec-discipline.md`, "Working tree and branch safety"). All findings below are escalated here instead of being applied. Re-run with the flag, or cut a branch first, to let the agent apply them.

## 2026-04-24 — spec-reviewer pass on f037ffd (REQ-READ-001 AC 5/6, REQ-HIST-001 AC 6)

Validated the 29-card cap + "see today" tile edit to REQ-READ-001 and the `?date=` deep-link edit to REQ-HIST-001 against commit f037ffd. Branch-safety on `main` blocks auto-commit; findings below are escalated.

- **Coverage (PASS):** REQ-READ-001 has 3 dedicated tests for the new AC 6 slot-30 tile (`tests/reading/digest-page.test.ts:187-213`) plus an updated feed test asserting 29 newest articles (`tests/articles/feed.test.ts:205`). REQ-HIST-001 has 3 dedicated tests for the new AC 6 deep-link (`tests/history/page.test.ts:120-145`). Both REQs: `test_count >= ac_count`. No tautologies, no `.skip`, no empty bodies in the new tests.
- **Changelog shape (PASS):** `sdd/changes.md` 2026-04-24 bullets 3 and 4 are dated, one sentence each, user-facing, no commit SHAs, no verification prose.
- **Forbidden content (LOW, one finding):** REQ-READ-001 AC 6 and the changelog bullet name the specific Material Design Icons token "gradient-vertical" — an icon-set implementation identifier analogous to a CSS class name. Proposed fix: replace "centred gradient-vertical icon" with "centred gradient-style icon" (or equivalent user-facing description) in both the AC and the changelog; move the concrete `M11,9H13V11...` / MDI name to `documentation/architecture.md`. No other implementation leakage detected (no file paths, hex codes, regex literals, or HTTP codes in the new AC text).
- **Round counter:** 1 of last 3 commits is agent-tagged (`[doc-updater]`); below the 2-of-3 hard stop.

## 2026-04-24 — REQ-PIPE-001 missing AC for article-body grounding (HIGH)

Commits 22fdc14, a29c60a, ab4facb, 25fe042 introduce a new observable behaviour: each ingested candidate carries a `body_snippet` captured from the feed entry's description/content when available, and when the feed snippet is thin or absent the coordinator/consumer fetches the article HTML (`src/lib/article-fetch.ts`, `Implements REQ-PIPE-001`) and extracts readable plaintext, so LLM summaries are grounded in real article content instead of hallucinated boilerplate. REQ-PIPE-001's current AC 1–6 do not mention snippet capture or body-fetch fallback. Proposed AC 7 to add: "Each candidate captures a body snippet from the feed entry when present; when the feed snippet is empty or too short to ground a summary, the article URL is fetched directly (HTTPS-only, SSRF-guarded, 5-second timeout, 1 MB response cap) and readable plaintext is extracted and attached to the candidate so the downstream LLM summary is grounded in real article content."

No test references `src/lib/article-fetch.ts` or asserts the snippet-vs-fetched-body path. Auto-promote/demote path: leaves REQ-PIPE-001 at `Implemented` today because tests still reference REQ-PIPE-001; but the NEW AC would ship without coverage — under the source-vs-test check this becomes a HIGH finding once the AC is added (Notes: "Body-fetch fallback ships in code but no test verifies the grounding path"). Next step for `tdd-guide`: write a test that feeds a candidate with an empty `body_snippet`, stubs `fetch` to return real article HTML, and asserts the consumer forwards the extracted text to the LLM prompt.

## 2026-04-24 — REQ-PIPE-002 AC bullets numbered out of order (LOW)

AC 7 (index-echo alignment) was inserted between AC 3 and AC 4 in the current revision, so the numbered list now reads 1, 2, 3, **7**, 4, 5, 6. The content is correct; only the ordering is wrong. Proposed fix: renumber inline so the new bullet becomes AC 4 and the existing AC 4/5/6 shift to AC 5/6/7. No cross-reference to AC numbers exists anywhere in `sdd/` or `documentation/` (verified via grep for `AC 4`, `AC 5`, etc.), so renumbering is safe. Cannot auto-apply because working branch is `main`.

## 2026-04-24 — Resolved: REQ-PIPE-002 AC 3 length target synced

Previous escalation proposed tightening AC 3 to "200–250 words in exactly 3 paragraphs". Commit 354df3f went the other direction — relaxed to "150–250 words across 2 or 3 paragraphs, each 3–5 sentences" — because the tighter target over-constrained gpt-oss-120b on thin-snippet candidates and produced padded output. The relaxed AC matches the current prompt contract in `src/lib/prompts.ts`. Changelog entry dated 2026-04-24 covers both the length relaxation and the new AC 7 index-echo rule. This item is resolved; no further action.

## 2026-04-24 — Items explicitly verified as NOT needing spec changes

- **Default model swap (f0a51bc):** REQ-SET-004 AC 3 refers to `DEFAULT_MODEL_ID` by constant name, not by model identifier. The constant moved from `@cf/google/gemma-4-26b-a4b-it` to `@cf/openai/gpt-oss-20b`; the REQ text is stable. `tests/lib/models.test.ts` already pins the new default. No REQ edit needed. The stale `sdd/changes.md` entry on line 36 referencing `gpt-oss-120b` is a historical entry — do NOT edit history.
- **Dev-gated `/api/dev/trigger-scrape` endpoint (30826b9):** operational / debug surface gated by `DEV_BYPASS_TOKEN`. Consistent with existing `/api/dev/login`. Not a product REQ.
- **`scripts/e2e-test.sh` full-cycle verification (30826b9):** CI / operational concern. No REQ-OPS-* covers CI harness scripts. Not a spec change.

## 2026-04-23 — missing REQ annotation in new source file

`src/pages/api/scrape-status.ts` (added in commit 892a5cd) implements observable behaviour covered by REQ-PIPE-006 AC 5 but contains no `Implements REQ-PIPE-006` comment. The source-vs-test check relies on the REQ ID literally appearing in a source file; without the annotation, spec-reviewer cannot match this endpoint back to its REQ in future runs. Add a top-of-file annotation. (Code-reviewer lane — spec-reviewer cannot touch source.)

## 2026-04-23 — AC 5 of REQ-PIPE-006 lacks automated coverage

The new scrape-status endpoint plus the reading/settings in-progress indicators (commit 892a5cd) ship without a test. REQ-PIPE-006 has been auto-demoted to Partial with Notes explaining the gap. Next step: `tdd-guide` to write tests exercising the endpoint's idle vs in-flight response shape and the UI indicator's visibility transitions, then promote back to Implemented.
