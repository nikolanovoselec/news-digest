# Review Needed

Items requiring human attention. Clear entries as they are resolved.

## 2026-04-25 — HIGH: Admin force-refresh endpoint has no REQ

**Trigger:** commit `b65339e` changed the GET response shape of `/api/admin/force-refresh` (browsers now get a 303 to `/settings`, scripts opt into JSON via `Accept: application/json`).

**Finding:** No REQ in `sdd/` documents the operator force-refresh contract. The source file `src/pages/api/admin/force-refresh.ts` carries `Implements REQ-OPS-004`, but REQ-OPS-004 is about crawler policy — the annotation is incorrect. The Admin actor is named in `sdd/README.md` as having "force a fresh scrape tick" capability, but no REQ defines the endpoint's behaviour, transports, or auth gate.

**Why escalated (not auto-fixed):** Creating a new REQ across domains is a JUDGMENT call. Conservative unleashed rule: never invent a new REQ when domain placement is ambiguous (observability vs generation).

**Proposed resolution (please pick one):**
1. Add `REQ-OPS-005: Admin force-refresh endpoint` in `sdd/observability.md` covering both transports (303 default, JSON on Accept), Cloudflare Access gating, and the REUSE_WINDOW_SECONDS guard. Update annotation in `force-refresh.ts` from `REQ-OPS-004` to `REQ-OPS-005`.
2. Fold the contract into a widened `REQ-GEN-002: Manual refresh` and split user-facing vs operator-triggered paths.
3. Treat as out-of-scope operator tooling and remove the misleading `REQ-OPS-004` annotation from the source file.

## 2026-04-26 — JUDGMENT: REQ-MAIL-001 AC 9 references the `email_enabled` column name

**Trigger:** spec-reviewer pass on `feat/rich-digest-email` (REQ-MAIL-001 redesigned from 5 to 10 ACs).

**Finding:** AC 9 reads ``Users who turn off `email_enabled` in settings receive no email.`` The token `email_enabled` is a database column name (and binding name), which the discipline rule lists under forbidden content for REQ ACs. The same token also appears in the domain header on line 3 of `sdd/email.md`.

**Why escalated (not auto-fixed):** This wording was preserved verbatim from the pre-redesign REQ — the user has had multiple chances to rephrase it and consistently kept it. Two competing readings:

- Strict reading: column-name leakage, replace with "Users who turn off the email-notifications toggle in settings receive no email."
- Contextual reading: `email_enabled` is also the user-visible setting key (the toggle name) and the contract noun shared between settings UI and email dispatcher. The discipline rule's allowlist already permits env-var names "when the env var IS the contract" — by the same logic the column name IS the contract here.

Conservative unleashed rule: do not overwrite intent on JUDGMENT calls.

**Proposed resolution (please pick one):**
1. Keep as-is. Add an entry to `sdd/.user-overrides.md` keyed `forbidden-content-column-name:REQ-MAIL-001` so future spec-reviewer runs do not re-raise this.
2. Rephrase AC 9 to "Users who turn off the email-notifications toggle in settings receive no email." and update line 3 of `sdd/email.md` similarly.

## 2026-04-26 — MEDIUM: REQ-MAIL-001 AC 9 lacks a direct test

**Trigger:** spec-reviewer test-coverage pass on `feat/rich-digest-email`.

**Finding:** AC 9 ("Users who turn off `email_enabled` in settings receive no email") is enforced by the SQL predicate `WHERE email_enabled = 1` in `src/lib/email-dispatch.ts` (lines 80 and 104), and the predicate's *presence* in the per-user SELECT is asserted indirectly by `tests/email/integration.test.ts:354` ("dispatcher SELECTs hashtags_json and tz from users"). No test sets a user with `email_enabled = 0` and asserts that the dispatcher skips them.

**Why escalated (not auto-fixed):** Writing tests is `tdd-guide`'s lane, not spec-reviewer's. The other 9 ACs all have explicit AC-named test coverage (AC 1, 3, 4, 5, 6, 7, 8, 10) or strong indirect coverage (AC 2 via the bucket-window matching tests). AC 9 is the only AC without a direct assertion.

**Proposed resolution:** Invoke `tdd-guide` to add one test in `tests/email/integration.test.ts`: "REQ-MAIL-001 AC 9: a user with `email_enabled = 0` is excluded from the per-tz scan and from the per-user SELECT." The fixture is a one-line variation of the existing once-per-day-gating tests.
