# Review Needed

Items requiring human attention. Clear entries as they are resolved.

## 2026-04-23 — TDD coverage gaps after cb4e3e9/525a8f4/1a3f0f6

spec-reviewer auto-demoted REQ-SET-002, REQ-READ-001, and REQ-GEN-006 from Implemented to Partial because the following acceptance criteria ship in code but have no test reference. Invoke `tdd-guide` to write failing tests first, then promote each REQ back once CI is green.

### REQ-SET-002 (settings — hashtag curation)
- **AC 2** — selecting a chip inverts its colour scheme, reveals the red × affordance, and `aria-pressed` toggles. Code: `src/pages/digest.astro` (around the tag-strip click delegator). Needed: a `tests/reading/digest-page.test.ts` (or new `tests/reading/tag-strip.test.ts`) assertion that the `?raw` source contains the selection-toggle handler and the inverted-chip CSS rules.
- **AC 4** — toggling selection never POSTs to the tags endpoint; only delete does. Needed: a unit test that imports the tag-strip script and confirms the handler path splits on "body click vs × click".
- **AC 7** — tag-strip selection filters the grid; empty selection shows every card; no match shows a "no stories match" region. Needed: a test asserting the `applyFilter` walk over `[data-digest-card]` and the empty-state region markup are present.
- **AC 8** — new accounts are seeded with `DEFAULT_HASHTAGS`; the settings page exposes a "Restore initial tags" action. Code: `src/lib/default-hashtags.ts`, `src/pages/api/auth/github/callback.ts:339`, `src/pages/settings.astro:381`. Needed: a callback test that asserts new-user INSERT binds the stringified default list, plus a settings-page test that the restore button POSTs `DEFAULT_HASHTAGS` to the tags endpoint.

### REQ-READ-001 (reading — overview grid)
- **AC 6** — per-card # affordance opens a non-interactive popover anchored to the card, auto-dismissing after 5 seconds or toggling off on a second activation. Code: `src/components/DigestCard.astro:259`. Needed: a `tests/reading/digest-page.test.ts` assertion on the popover markup, the 5 s timer, and the second-click dismiss.
- **AC 7** — described under REQ-SET-002 AC 7 (same filter code path).

### REQ-GEN-006 (generation — atomic final write)
- **AC 5** — each article row persists its validated tag list alongside the article. Code: `src/lib/generate.ts` (insert binding), `migrations/0002_article_tags.sql`. Needed: a pipeline test that asserts the batch insert binds the tags payload and that the today-API response carries `tags[]` per article.

### Not a finding — verified
- REQ-GEN-005 AC 6 (tags echo + validate + fallback) is covered by `tests/generate/sanitize.test.ts` (4 tag-validation cases) and `tests/lib/prompts.test.ts` DIGEST_SYSTEM AC-6 test.
- REQ-GEN-005 AC 7 (NYT 45–80 headline) is covered by `tests/lib/prompts.test.ts` DIGEST_SYSTEM AC-7 test.
- REQ-DES-001 / REQ-DES-003 promotion to Implemented is valid — `tests/design/visual-language.test.ts` + `tests/design/motion-system.test.ts` both reference the REQ IDs.

## Round counter

0 agent commits in the last 3 at this run. Free to auto-fix on the next round if the TDD gaps are closed.
