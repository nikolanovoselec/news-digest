# Review Needed

Items requiring human attention. Clear entries as they are resolved.

## 2026-04-23 — Promotion sweep complete

spec-reviewer post-coverage sweep promoted REQ-READ-006, REQ-OPS-004, REQ-PWA-003, and REQ-SET-002 to Implemented after the new test files under `tests/reading/`, `tests/observability/`, `tests/pwa/`, and `tests/settings/` landed with REQ-ID references covering the previously-unverified AC. REQ-DISC-002 and REQ-DISC-003 remain Partial with Notes refined to distinguish covered AC from deferred scope (tracked in pending.md, not a test-writing gap).

### REQ-SET-004 (settings — model selection)
Stays Partial for product reasons (Notes in the REQ): the global pipeline uses a single default model, so the model picker is hidden in the UI while the underlying code remains. No test action required — this is a behaviour decision, not a coverage gap.

### Out-of-scope finding (not addressed this sweep)

Four REQs still claim `Status: Implemented` with no test or source references anywhere in the repo:

- REQ-MAIL-002 (non-blocking email failure)
- REQ-GEN-002 (manual refresh with rate limiting) — already flagged "Superseded by REQ-PIPE-*"
- REQ-GEN-007 (stuck-digest sweeper) — already flagged "Superseded by REQ-PIPE-*"
- REQ-GEN-008 (cost/time/token transparency) — already flagged "Superseded by REQ-PIPE-*"

REQ-GEN-002/007/008 should likely follow REQ-GEN-006 into Deprecated (Replaced By: REQ-PIPE-*). REQ-MAIL-002 needs either a test or a move to Partial. Not auto-fixed this sweep — out of scope for the promotion task, and auto-deprecating three REQs at once risks a spiral false-positive.

## Round counter

Round 1 after the test-coverage push. Non-agent commits dominate the recent history, so the round counter is fresh.
