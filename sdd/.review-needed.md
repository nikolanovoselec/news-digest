# Review Needed

Findings escalated by spec-reviewer or doc-updater that require human attention. Resolve and clear entries as they are addressed.

## Notes

- Three earlier MEDIUM JUDGMENTs from the 2026-05-03 `/sdd clean` run are resolved:
  - **Cookie-attribute leakage** in REQ-AUTH-002 / REQ-AUTH-003 — recorded as **AD8: Cookie attributes are the security contract**.
  - **D1/KV shape leakage** in REQ-DISC-001/002, REQ-AUTH-002, REQ-SET-001/005 — recorded as **AD9: Storage-shape names are the persistence contract**.
  - **Architecture ASCII diagrams over the 15-line per-element cap** — annotated inline in `documentation/architecture.md` with `<!-- doc-allow-large -->` markers above each fenced diagram. Diagrams kept as-is.

- JUDGMENT resolutions go in ADRs (`documentation/decisions/`), not `sdd/.user-overrides.md`. The override file is deprecated by [codeflare#266](https://github.com/nikolanovoselec/codeflare/issues/266); spec-reviewer reads each ADR's `Overrides:` header to honour skips.

## 2026-05-06 — All previously escalated findings resolved

The five findings open before this sweep have been addressed:

- **MEDIUM (PR #201) — REQ-READ-001 AC 7 has no direct test coverage by REQ ID** → resolved. The existing wire-shape case in `tests/history/api.test.ts` was renamed to `REQ-HIST-001 / REQ-READ-001 AC 7: alt_source_count …` so spec-reviewer's REQ-grep counts it for both. A new pure-formatter helper at `src/lib/alt-source-label.ts` (`formatAltSourceLabel`) extracts the `+N` suffix and aria-label string production, with a 4-case unit test at `tests/lib/alt-source-label.test.ts` pinning the singular/plural aria-label and the no-suffix-on-zero contract. `DigestCard.astro` consumes the helper.
- **LOW (PR #201) — Stale REQ-PIPE-001 AC 2 wording vs MAX_CANDIDATES_PER_CHUNK = 25** → resolved. AC 2 rewritten to "partitions candidates across one or more chunk jobs whose size is capped to fit the model's context window," dropping the literal `~100` anchor. REQ-PIPE-002 Intent updated in the same vein ("batches sized to fit the model's context window").
- **MEDIUM (2026-05-05) — Two parallel Google News paths, ownership unclear** → resolved by **AD31** ("Google News baseline ownership lives at the coordinator, not in discovery"). Coordinator owns per-tag GN fan-out; discovery LLM's GN fallback is redundant during transition; follow-up issue tracks the prompt retrain.
- **MEDIUM (2026-05-05) — Per-item source-name labelling has no AC home** → resolved. REQ-PIPE-001 AC 4 extended with the per-item-publisher rule (Option A from the original recommendation: the label resolution lives with the source-list construction REQ, not on the reading surface).
- **MEDIUM (2026-05-05) — REQ-PIPE-008 AC 1 silent on the "different studies don't merge" rule** → resolved. The studies/audits/benchmarks non-merge rule is now its own bullet at REQ-PIPE-008 AC 11 (initially appended to AC 1, but spec-reviewer's run-on-AC check in the next sweep moved it to a standalone AC so existing AC 2-10 cross-references in source/tests/ADRs/changelog stay stable). Scope locked to studies/audits/benchmarks, not generalised to "any content with conflicting numbers" — that wider scope is left for a future pass if regressions show up beyond research papers.
- **LOW (2026-05-05) — `googleNewsSourceForTag` tests do not name a REQ ID** → resolved. The describe block was renamed to `googleNewsSourceForTag — REQ-PIPE-001 AC 9 auto-synthesised per-tag GN feeds`.

No findings open. The next spec-reviewer / doc-updater pass starts from a clean slate.

## 2026-05-06 — doc-updater PR #206 pass

**MEDIUM — table cell over 50-word cap (doc-discipline Pass 1)** — RESOLVED (PR #227)

`scrape-finalize-consumer.ts` role column trimmed to 36 words in this PR's architecture.md changes.

## 2026-05-07 -- doc-updater PR #215 pass: hard stop (anti-spiral)

All 3 of the last 3 commits are agent-authored ([doc-updater], [unleashed] [spec-reviewer] x2). Round counter limit reached (>=2 of last 3).

doc-updater did not run any validation or apply any fixes this pass. No findings were produced or suppressed.

**Action required:** make a non-agent commit (real code or manual edit) to reset the counter, then re-trigger the review pipeline if further doc validation is needed.

## 2026-05-07 — spec-reviewer PR #230 release pass (auto mode)

One HIGH finding was auto-fixed in this pass; two LOW findings are deferred for the next `/sdd clean` run.

**HIGH (RESOLVED) — REQ-PIPE-009 AC 5 described a per-invocation numeric cap that no longer exists in shipped code.**

AD36 (2026-05-07) removed the per-batch rerank cap (`MAX_RERANKS_PER_BATCH = 4`) from `runHistoricalDedupBatch` in favour of the queue consumer's wall-clock budget. AC 5 was rewritten in this pass to describe the wall-clock-bounded behaviour without naming a numeric cap, so the spec now matches the shipped semantics. AD36 documents the rationale; the AC describes the operator-observable contract.

**LOW (deferred) — Changelog entry on 2026-05-07 line 11 has no corresponding AC delta in this diff.**

The first 2026-05-07 bullet in `sdd/changes.md` ("REQ-PIPE-003 + REQ-PIPE-009 recalibrated against a 2026-05-07 prod audit ...") describes a numeric threshold tuning. Per the discipline rule, threshold values are correctly absent from REQ ACs (the ACs use abstracted phrases like "auto-merge threshold" and "borderline band"); the rationale lives in AD36. Because no user-observable AC changed in REQ-PIPE-003 / REQ-PIPE-009 as a direct consequence of the recalibration (the AC 9 redelivery clause and AC 9 background-runs reshape are separately covered by their own bullets at lines 12 and 15), the recalibration bullet is changelog drift. Proposed cleanup: drop the first bullet and rely on AD36 for the rationale; the AC 5 rewrite that just landed gets its own bullet ("REQ-PIPE-009 AC 5 reworded: the rerank invocation bound is wall-clock based rather than a fixed count of judgments").

**LOW (deferred) — REQ-OPS-008 Notes field embeds a source file path.**

`sdd/observability.md` line 177 reads `Notes: UI orchestrator and cross-navigation persistence ship in 'src/pages/settings.astro'; no automated test references REQ-OPS-008 yet, so coverage is binary-undetected.` The file path is forbidden content per the spec discipline. Proposed cleanup at the next `/sdd clean`: rewrite to `Notes: UI orchestrator and cross-navigation persistence have shipped, but no automated test references REQ-OPS-008 yet, so coverage is binary-undetected.` Drop the file path; `documentation/architecture.md` is the right home for the source-file mapping.

## 2026-05-12 — spec-reviewer cycle-1 review bundle pass (unleashed mode)

The 2026-05-12 changelog entries (commit 758a1a9) claimed five AC rewordings/extensions but the diff against `origin/main` showed no AC text changes. This pass closed that gap by actually updating the ACs to reflect the shipped code in PR #246. Behavioural observation: REQ-PIPE-001 AC 10, REQ-PIPE-003 AC 18, REQ-PIPE-006 AC 8, REQ-AUTH-001 AC 8d, AC 9g, and AC 10 are all new (additions, not rewordings); REQ-AUTH-001 AC 8a and AC 9 are extensions. The 2026-05-12 changelog block was rewritten so the entries describe what actually changed (e.g., "AC 10 added" instead of "AC reworded") and the two missing entries (orphan scrape_runs sweep, dev-route prod guard) were added.

**MEDIUM (escalated to code-reviewer's lane) — `src/queue/cleanup.ts` lacks a `// Implements REQ-PIPE-006` annotation despite `runOrphanScrapeRunSweep` implementing REQ-PIPE-006 AC 8.**

Spec-reviewer cannot edit source code. Annotation addition is in the code-reviewer / refactor-cleaner lane per the spec-discipline annotation rules. Suggested next edit: append `// Implements REQ-PIPE-006 (orphan scrape_runs sweep)` to the header block of `src/queue/cleanup.ts`. Binary REQ-grep detection still passes today because the source-vs-test rule only checks file-level substring presence, but the explicit annotation is the convention that downstream agents rely on. No status change recommended for REQ-PIPE-006 — `tests/pipeline/cleanup.test.ts` already references it indirectly through CF-008 cases and `runOrphanScrapeRunSweep` is exercised by the cleanup integration tests.

**LOW (deferred) — REQ-PIPE-001, REQ-PIPE-003, and REQ-AUTH-001 now sit in the 26-50-line LOW band.**

REQ-PIPE-001 is 31 lines (10 ACs), REQ-PIPE-003 is 34 lines (18 ACs), REQ-AUTH-001 is 38 lines (10 ACs with several sub-bullets). Per the unleashed-mode conservative rule, oversized REQs are shrunk by extracting implementation prose to `documentation/{relevant-file}.md` — but these REQs contain no implementation prose to extract; the length is from genuine AC surface area. No auto-fix applied. The LOW band is informational; the next `/sdd clean` pass can decide whether REQ-PIPE-003 (18 ACs, the upper bound on what fits as a single concern) should be split, which is a JUDGMENT call requiring user input on the natural boundary.

**LOW (informational) — test coverage gap for the new AC 10 in REQ-PIPE-001 (scrape_wait cap).**

`tests/queue/pipeline-consumer.test.ts` exercises the `scrape_wait` phase machinery (advance, re-enqueue, finalize) but has no case that asserts the run is marked `failed` after the configured iteration budget is exceeded. Binary REQ-grep detection still passes because the file references REQ-PIPE-001 and tests the consumer that implements it. Suggested follow-up: add an `it('REQ-PIPE-001 AC 10: scrape_wait exceeding budget marks run failed', ...)` case that drives the consumer past the iteration cap and asserts `current_phase = 'done'` plus `status = 'failed'` on the scrape_runs row. Not auto-fixed because spec-reviewer does not write tests; this is on the tdd-guide / next dev pass.
