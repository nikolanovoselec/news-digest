# Review Needed

Items requiring human attention. Clear entries as they are resolved.

## 2026-04-27 — MEDIUM: Five rate-limited source files lack `Implements REQ-AUTH-001` annotation

**Trigger:** commits `6be081a` (wire RATE_LIMIT_RULES) and `7e6f3ea` (close TAGS_MUTATION bypass on PUT /api/settings) wired `enforceRateLimit` into authenticated mutation endpoints. REQ-AUTH-001 AC 9 is the spec contract these endpoints now enforce.

**Finding:** Five source files now implement REQ-AUTH-001 AC 9 but do not declare it:

- `src/pages/api/articles/[id]/star.ts` — header reads `// Implements REQ-STAR-001`; needs `REQ-AUTH-001` added
- `src/pages/api/settings.ts` — header reads `// Implements REQ-SET-001..REQ-SET-006`; needs `REQ-AUTH-001` added
- `src/pages/api/tags.ts` — header reads `// Implements REQ-SET-002, REQ-AUTH-003`; needs `REQ-AUTH-001` added
- `src/pages/api/tags/delete-initial.ts` — header reads `// Implements REQ-SET-002 AC 8 + REQ-AUTH-003`; needs `REQ-AUTH-001` added
- `src/pages/api/tags/restore.ts` — same as delete-initial; needs `REQ-AUTH-001` added

Without the annotation, the source-vs-test grep that spec-reviewer runs every push has nothing to match on these files — silent code-vs-spec drift can land unnoticed.

**Why escalated (not auto-fixed):** Source code is out of lane for spec-reviewer.

**Proposed resolution:** Append `, REQ-AUTH-001` to the existing `// Implements …` line on each of the five files, in a follow-up commit. Code-reviewer should also flag this on future pushes.

## 2026-04-27 — MEDIUM: Two new rate-limit tests don't name REQ-AUTH-001

**Trigger:** commits `6be081a` and `7e6f3ea` added 429 contract tests for the per-user `TAGS_MUTATION` and per-user `ARTICLE_STAR` buckets.

**Finding:** Two test cases pin REQ-AUTH-001 AC 9 behaviour but neither file mentions REQ-AUTH-001 in any test title or comment:

- `tests/settings/api.test.ts` — `it('CF-028: returns 429 when the per-user TAGS_MUTATION bucket is exhausted (PUT closes the bypass)')` — `grep` of file: 0 matches for `REQ-AUTH-001`
- `tests/articles/star.test.ts` — the new 429 test — `grep` of file: 0 matches for `REQ-AUTH-001`

Substring-grep test discovery (5a/5b/5c) cannot associate these tests with REQ-AUTH-001. Tests for the same AC do exist elsewhere (`tests/settings/delete-initial.test.ts` line 10 mentions REQ-AUTH-003, no REQ-AUTH-001 in its 429 case either), so AC 9 has no test reference under spec-reviewer's eyes even though the behaviour is well-pinned in code.

**Why escalated (not auto-fixed):** Test files are out of lane for spec-reviewer.

**Proposed resolution (please pick one):**
1. Rename the test titles to lead with the REQ ID, e.g. `'REQ-AUTH-001 AC 9: returns 429 when the per-user TAGS_MUTATION bucket is exhausted'`. This is the convention every other test file in the suite already follows.
2. Add a single `// Implements REQ-AUTH-001 AC 9` comment block at the top of each file's `describe(...)`. Title untouched, grep matches.
3. Both — title rename plus a file-level annotation. Recommended for surfaces where multiple ACs converge.

## 2026-04-27 — MEDIUM: Test name says REQ-PIPE-002 but pins REQ-PIPE-006 contract

**Trigger:** commit `a0a345c` added test `'REQ-PIPE-002: addChunkStats is gated by completion INSERT — no double-count under redelivery'` in `tests/pipeline/chunk-consumer.test.ts`.

**Finding:** The behavior the test pins — per-tick counters not double-counting under queue redelivery — is now spelled out in REQ-PIPE-006 AC 7 (added this push). The test's title still names REQ-PIPE-002, which is the consumer's primary REQ but does not contain the idempotency contract. Spec-reviewer cannot auto-fix this without touching test code (out of lane).

**Why escalated (not auto-fixed):** Renaming tests is the developer's call; spec-reviewer never edits source/test files. The test-quality heuristic (5c) is also not violated — REQ-PIPE-006 already has multiple tests referencing its ID elsewhere in the suite, so the AC-count vs test-count check passes.

**Proposed resolution (please pick one):**
1. Rename the test to `'REQ-PIPE-006: addChunkStats is gated by completion INSERT — no double-count under redelivery'` so grep-based coverage tooling associates it with the AC it pins.
2. Add a duplicate REQ-ID prefix in a comment inside the test body (e.g. `// Also pins REQ-PIPE-006 AC 7`) and leave the title as-is. Substring grep will then match REQ-PIPE-006 inside the file.
3. Do nothing — the file-level annotation pattern is good enough and the test's primary subject (addChunkStats called from processOneChunk) is genuinely REQ-PIPE-002 implementation.

## 2026-04-25 — HIGH: Admin force-refresh endpoint has no REQ

**Trigger:** commit `b65339e` changed the GET response shape of `/api/admin/force-refresh` (browsers now get a 303 to `/settings`, scripts opt into JSON via `Accept: application/json`).

**Finding:** No REQ in `sdd/` documents the operator force-refresh contract. The source file `src/pages/api/admin/force-refresh.ts` carries `Implements REQ-OPS-004`, but REQ-OPS-004 is about crawler policy — the annotation is incorrect. The Admin actor is named in `sdd/README.md` as having "force a fresh scrape tick" capability, but no REQ defines the endpoint's behaviour, transports, or auth gate.

**Why escalated (not auto-fixed):** Creating a new REQ across domains is a JUDGMENT call. Conservative unleashed rule: never invent a new REQ when domain placement is ambiguous (observability vs generation).

**Proposed resolution (please pick one):**
1. Add `REQ-OPS-005: Admin force-refresh endpoint` in `sdd/observability.md` covering both transports (303 default, JSON on Accept), Cloudflare Access gating, and the REUSE_WINDOW_SECONDS guard. Update annotation in `force-refresh.ts` from `REQ-OPS-004` to `REQ-OPS-005`.
2. Fold the contract into a widened `REQ-GEN-002: Manual refresh` and split user-facing vs operator-triggered paths.
3. Treat as out-of-scope operator tooling and remove the misleading `REQ-OPS-004` annotation from the source file.

