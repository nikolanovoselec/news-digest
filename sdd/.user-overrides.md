# User Overrides

Entries here record explicit user decisions that override what spec-reviewer's default rules would otherwise do. Each entry is keyed by `{rule_id}:{target_id}` so spec-reviewer can skip the matching finding silently on subsequent runs.

## forbidden-content-column-name:REQ-MAIL-001
- Date: 2026-04-26
- User note: `email_enabled` in REQ-MAIL-001 AC 9 (and the domain header on line 3 of `sdd/email.md`) is intentionally preserved as the contract noun shared between the settings UI toggle, the `users` column, and the dispatcher's SQL predicate. The column-name leak rule does not apply here for the same reason env-var names are allowed when the env var IS the contract — `email_enabled` is the user-visible identity of the feature, not an implementation detail. Do not rephrase.
- Skip: yes

## skipped-test:REQ-READ-002,REQ-HIST-001
- Date: 2026-04-28
- User note: The numeric perf-comparability test (history back-nav ≤ 1.6× digest back-nav, median of 3 samples) in `tests/e2e/view-transition.spec.ts` is permanently skipped. The /history page is structurally slower than /digest because opened day-groups carry more cards and trigger more layout work — that's accepted as the cost of the feature, not a regression. The structural REQ-READ-002 / REQ-HIST-001 contracts (single-named-group view-transition shaping, return-morph pair forms at astro:after-swap) remain covered by the non-skipped tests above the skip in the same file. Resurrect the perf test only if a future refactor claims to close the gap and a numeric guard is wanted. User-confirmed on 2026-04-28: "we leave it as is, enough trying to fix this."
- Skip: yes

