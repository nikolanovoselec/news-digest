# User Overrides

Entries here record explicit user decisions that override what spec-reviewer's default rules would otherwise do. Each entry is keyed by `{rule_id}:{target_id}` so spec-reviewer can skip the matching finding silently on subsequent runs.

## deprecated-with-live-code:REQ-DISC-002
- Date: 2026-04-24
- User note: Discovery progress banner was partially built (AC 1 endpoint shipped) but de-scoped. User explicitly chose Deprecated over reverting to Planned; the endpoint code remains so downstream consumers don't break, but the feature is no longer on the active backlog.
- Skip: yes

## deprecated-with-live-code:REQ-SET-004
- Date: 2026-04-24
- User note: ModelSelect UI is hidden (`showModelSelect = false`) and the pipeline runs on a single global DEFAULT_MODEL_ID. The backend catalog, validator, and tests are preserved so the feature can be flipped back on without a schema change, but the product no longer exposes it. User explicitly chose Deprecated. Do not auto-revert to Partial/Implemented based on the live code/tests.
- Skip: yes

## forbidden-content-column-name:REQ-MAIL-001
- Date: 2026-04-26
- User note: `email_enabled` in REQ-MAIL-001 AC 9 (and the domain header on line 3 of `sdd/email.md`) is intentionally preserved as the contract noun shared between the settings UI toggle, the `users` column, and the dispatcher's SQL predicate. The column-name leak rule does not apply here for the same reason env-var names are allowed when the env var IS the contract — `email_enabled` is the user-visible identity of the feature, not an implementation detail. Do not rephrase.
- Skip: yes
